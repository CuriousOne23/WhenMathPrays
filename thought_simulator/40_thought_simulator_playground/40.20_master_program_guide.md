# 40.20_Master Program Guide

## Purpose

This document is the **governing constitution** of the `40_thought_simulator_playground/`.

It defines the philosophy, rules, structure, and workflow we follow when exploring and building the Thought Simulator. It ensures every module is developed with scientific rigor, humility, determinism, and full traceability.

For user-facing forward/backward flow runbooks and prompt templates, see `../USER_GUIDE.md`.

## Core Philosophy

We are **explorers**, not experts pretending to know everything upfront.

The Thought Simulator is a novel, conceptual, emergent system. We cannot design it perfectly on paper. Therefore we:

- Stay humble about what we don't know  
- Break the unknown into small, focused modules  
- Prototype quickly and safely  
- Observe, test, break, and learn  
- Refine requirements and design based on evidence  
- Document insights, failures, and changes as we go  
- Only promote stable, verified modules to the final `50_thought_simulator_design/` folder  

This is the scientific method applied to software engineering.

---

# Playground Structure

Every module follows this exact layout:

```
<module_name>/
|-- software_description.md          # Tentative design sketch
|-- prototype.py                     # Macro-style exploratory implementation
|-- harness.py                       # Test / verification runner
|-- verification_capsule.md          # Canonical verification report and migrated history
|-- requirements_delta.md            # Requirement evolution and implementer feedback
`-- artifacts/                       # Run artifacts, snapshots, and reproducibility evidence
```

The canonical verification vocabulary is defined in `../30_verification/30.30_verification_glossary.md` and shared by all modules.

---

# Scaffold Governance for Planned 40.* Expansion

Planned exploratory expansion in `40_thought_simulator_playground/` may require creating new subdirectories, placeholder modules, and dummy files before full implementation is available.

This is allowed only under explicit scaffold governance.

## Allowed Scaffold Types

- planned module directories that follow the standard 40.* module layout
- placeholder `software_description.md` files defining intended scope and requirement targets
- placeholder `requirements_delta.md` files recording missing implementation and requirement gaps
- empty or stub `prototype.py`/`harness.py` files only when marked as non-executable scaffolds

## Mandatory Scaffold Metadata

Every scaffold module must declare at minimum:

- `scaffold_status`: one of `planned`, `exploratory`, `validated`, `promoted`
- intended requirement anchors in `20_requirements/` and `10_thought_simulator_req/10_system_architecture/`
- applicability note describing why the scaffold exists and what future capability it enables
- disposition target (`promote`, `replace`, or `archive`)

## Compliance and Promotion Rules

- Scaffold files are not treated as compliant by default.
- A scaffold may be considered compliant only after required implementation/evidence gates pass and status is upgraded to `validated`.
- Any scaffold promoted to design must satisfy the same HLR/LLR traceability and evidence requirements as non-scaffold modules.

## Post-40.20 Review and Canonical 10.50 Request Rule (Mandatory)

After a user requests execution under this 40.20 guide for a `40.*` module, the AI Agent must enforce the following sequence:

1. Execute module work in the target `40.*` directory and produce verification evidence.
2. Stop for human review in that same `40.*` directory.
3. Do not perform canonical requirement creation/update in `10_thought_simulator_req/50_design/` until explicit human approval is provided.
4. After approval, require an explicit follow-up request to perform `10.50` canonical anchor action.

`10.50` canonical anchor action is conditional:

- if an appropriate `10.50.*` anchor already exists, update/reuse that anchor (do not create a duplicate)
- if no appropriate `10.50.*` anchor exists, create a new `10.50.*` anchor under controlled promotion with approval recorded

No automatic `10.50` creation is allowed as part of the initial `40.20` execution request.

## Archive Disposition Rule

- If a scaffold becomes obsolete, duplicate, or no longer applicable to current 20/10.10 constraints, it must be moved to a layer-local archive under `40_thought_simulator_playground/archive/`.
- Legacy scaffold files must not remain mixed with active exploratory modules.
- Archive moves must be recorded in the active backward-flow/alignment log with rationale.

---

# Handling Missing Requirement Coverage (Exploratory Modules Only)

Some modules in `40_thought_simulator_playground/` explore subsystems that do not yet have complete requirement coverage in `20_requirements/`.
This is expected for early-stage or newly introduced subsystems.

Terminology in this guide: **HLR** means **High-Level Requirement** (system-level requirement), and **LLR** means **Low-Level Requirement** (software-design-level requirement).

When a module has missing requirement coverage, the AI Agent must follow these rules:

1. **Reference only existing requirement documents.**
  The module's `software_description.md` must list only the requirement files that actually exist in the repository.

2. **Declare missing requirement coverage explicitly.**
  If a subsystem has no dedicated requirement document, the module must state this in its Source Index.

3. **Record gaps in `requirements_delta.md`.**
  Missing, unclear, or newly discovered requirements must be captured as deltas rather than assumed or invented.

4. **Use placeholder requirement IDs temporarily.**
  When no HLR/LLR exists, the AI Agent must use `HLR-?` and `LLR-?` and record the gap in both the verification capsule and the requirement delta.

5. **Do not create requirement documents automatically.**
  New requirement documents may be created after the design and delta trail stabilizes and only with architect approval.

6. **Design specifications may precede requirement documents for exploratory modules.**
  For subsystems without requirement coverage, a design spec in `50_thought_simulator_design/` may be created based on lifecycle descriptions and verification evidence before a requirement document exists.

These rules apply only to exploratory modules with missing requirement coverage. Established subsystems continue to follow the standard requirements -> design -> implementation -> verification pipeline.

## Controlled Backward Canonical Update Flow (10 -> 30/40/50)

Backward propagation from canonical requirement anchors in `10_thought_simulator_req/` into verification, playground, and design layers is allowed, but only under explicit control.

No-auto-propagation from 20-layer edits:

- changes in `20_requirements/` must not automatically trigger cross-layer updates
- AI Agent may only provide impact analysis and request explicit user confirmation to run forward/backward flow
- execution starts only after explicit user instruction

Allowed automatic maintenance (30/40/50 scope, after explicit flow start):

- synchronize glossary terms and glossary registry entries for terminology changes in 30/40/50 docs
- repair README and markdown link/path references after file rename or section-name changes
- synchronize `50.00_design_traceability_index.md` when 50-series file identity or mapping changes

10-layer disambiguation for this guide:

- `10_thought_simulator_req/` = canonical requirement trigger source.
- `00_program_governance/` = architecture/program reference context.

When this section says "update 10" or "10-layer update", it refers to `10_thought_simulator_req/` unless explicitly qualified.

### Trigger Conditions

Use backward flow only when a canonical-anchor change in `10_thought_simulator_req/` materially affects:

- verification vocabulary or evidence mapping in `30_verification/`
- exploratory module contracts or requirement attachment in `40_thought_simulator_playground/`
- design contracts or traceability mappings in `50_thought_simulator_design/`

Mandatory TR alignment trigger:

- if changed canonical anchors include `10.10.10`, `10.10.20`, `10.10.40`, or `10.10.50` with TP.TR and `TP.tr_needs_update` semantics, impacted playground modules must update terminology, routing assumptions, and requirement attachment before promotion

### Source-Lineage Clarification (20 -> 10 -> 30/40/50)

When canonical anchors in `10_thought_simulator_req/` were promoted or updated from architectural guidance in `20_requirements/`, backward-flow propagation must still treat the 10-layer anchor as the normative trigger source.

Required interpretation rule:

- `20_requirements/` provides architectural rationale and terminology baseline
- `10_thought_simulator_req/` provides canonical requirement authority for downstream propagation
- downstream update flow executes from 10 to 30/40/50, with 20 cited only as rationale lineage where relevant

### Required Backward-Flow Proposal Packet

Before applying downstream edits, create a proposal packet (can be a markdown log) containing:

- changed canonical anchor paths in `10_thought_simulator_req/`
- concise change rationale and risk statement
- impacted 30/40/50 files
- required traceability updates (HLR/LLR mapping, glossary, capsules, deltas, design refs)
- confirmation checklist and approver section

### Confirmation Gate (Mandatory)

No backward-flow propagation may proceed past planning unless:

- the proposal packet is complete
- human owner confirms proceed/approved
- impacted module owners (or designated reviewer) are identified

### Ambiguous Flow-Direction Rule (Mandatory)

If a request can be interpreted as either forward flow (promotion from exploratory layers toward canonical/design stabilization) or backward flow (canonical-anchor-driven propagation into downstream layers), the AI Agent must pause and request explicit human confirmation of flow direction before making edits.

Minimum confirmation record:

- selected flow direction: `forward` or `backward`
- initiating source layer/document
- expected impacted target layers/documents

If confirmation is not provided, execution must stop at planning/reporting only.

### Execution Order (Mandatory)

When approved, apply updates in this order:

1. Canonical source update in `10_thought_simulator_req/` (normative source)
2. Verification alignment in `30_verification/` (capsule/delta/glossary/trace links)
3. Playground alignment in `40_thought_simulator_playground/` (requirements_delta, capsule mapping, terminology)
4. Design alignment in `50_thought_simulator_design/` (spec contracts and index mappings)

### Release Blocking Rules

Backward-flow updates are considered incomplete and not promotion-eligible if any of the following remain unresolved:

- stale requirement references in capsules/deltas/specs
- glossary mismatch between canonical and verification layers
- unmapped HLR/LLR references after canonical change
- traceability index rows not synchronized with updated anchors

### Required Record Artifacts

Each backward-flow execution must leave:

- one alignment log in the initiating layer (`10_thought_simulator_req/docs/` preferred)
- updated affected `requirements_delta.md` and `verification_capsule.md` entries where applicable
- explicit cross-layer file list of what changed and why
- explicit lineage note indicating whether initiating anchors were updated from `20_requirements/`

### Automatic Controls on Backward-Flow Request (Mandatory)

When backward flow is requested after a `20_requirements/` to `10_thought_simulator_req/` canonical update, the AI Agent must automatically execute both controls below without requiring a second request:

1. create or update the backward-flow execution log in `10_thought_simulator_req/docs/`
2. run a post-update integrity check for impacted 30/40/50 documents and record pass/fail summary in that log

Minimum integrity-check scope:

- stale requirement references after propagation
- glossary mismatch against canonical verification terminology controls
- unmapped HLR/LLR references introduced by the update

TR-specific integrity additions:

- verify all impacted playground docs avoid legacy assumptions that TR always runs unconditionally
- verify RB/TR wording reflects: RB routes to TR iff `TP.tr_needs_update=true`
- verify dirty-flag lifecycle wording reflects: initialize true, semantic writers set true, clear only on successful TR execution, preserve true on TR failure

Full design synchronization completion gate:

- backward-flow completion requires full synchronization review across all files in `50_thought_simulator_design/`
- `50.00_design_traceability_index.md` must be synchronized in the same transaction if any design mappings change
- execution log must record final assertion `Forward-Equivalence State: YES`

# Prototype Rules (Macro-Style Module)

`prototype.py` must be written as a **macro-style module**.

### Definition of Macro-Style Module
A **macro-style module** is:

- **Self-contained**  
- **Importable** (no top-level execution)  
- **Reusable** in any environment  
- **Deterministic** when deterministic mode is enabled  
- **Encapsulated** (all state inside classes)  
- **Stateless at module level** (no global mutable state)

### Required Properties

- Exposes a clean **public API**  
- Contains **internal helpers**  
- Defines **state variables** inside classes  
- Uses **local variables** freely inside functions  
- Has **no code at top level**  
- Has **no global mutable state**  
- Must be compatible with `harness.py`  

This ensures reusability, testability, determinism, and parallel safety.

---

# IO Variable and Function Contract Standard

Every module must define a formal IO contract in its `software_description.md` for `prototype.py`.

## Required Coverage

The IO contract must explicitly define:

- inbound variables (inputs), including type, constraints, and purpose
- outbound variables (outputs), including type, constraints, and consumer expectations
- variable identity semantics (what each key state variable means)
- formatting and serialization rules for cross-program interoperability
- applicability to other programs (who can produce/consume the data)

## Minimum Required Attributes per IO Variable

Each IO variable entry must include:

- variable name
- direction (`input` or `output`)
- type
- required constraints (range, non-empty, shape, determinism expectations)
- function role (what it does in the lifecycle)
- interoperability notes (how external programs must treat it)

## Function-Level IO Definition

For each public API function in `prototype.py`, document:

- function name
- required input variables and optional input variables
- output variables and side effects
- deterministic behavior conditions
- failure/validation behavior for invalid inputs

## Formatting Rules for Inter-Program Exchange

- Primary interchange format is JSON-compatible dictionary structures.
- Numeric vectors must serialize as plain lists of numbers.
- No consumer may depend on internal Python-only object identity.
- Preserve canonical identity fields (`tp_id`, `state_counter`, provenance IDs) without transformation.
- If schema evolves, add versioning or backward-compatible optional fields.

## Inbound vs Outbound Function Semantics

- Inbound variables control state transition intent (create, move, update, split, merge).
- Outbound variables provide verifiable state evidence (object state, history, provenance, counters).
- Harness and external tools must treat outbound data as read-only evidence.
- Any adapter that transforms outbound data must be lossless for required identity fields.

## Traceability Requirement

The module Run Record and Verification Ledger must reference which IO contract fields were exercised by each test scenario when applicable.

---

# Variable Control Rules

## Global Variables
**Forbidden** (except constants in `UPPER_CASE`).

- No module-level mutable variables  
- No shared caches  
- No static mutable fields  
- No hidden counters  
- No hidden registries  

**Rationale:**  
Global mutable state destroys determinism, reproducibility, parallelism, and verifiability.

---

## State Variables
Allowed **only inside classes** (e.g., inside `ThoughtPoint`).

State variables must be:

- Explicit  
- Observable  
- Deterministic  
- Updated only through public API methods  
- Logged by the harness when changed  

Examples:

- entropy  
- embedding  
- energy  
- basin_id  
- tags  
- provenance  
- history  
- state_counter  

---

## Local Variables
Allowed and encouraged.

Local variables must:

- Remain ephemeral  
- Never escape function scope  
- Never mutate global state  
- Never become persistent state  

Examples:

- entropy deltas  
- temporary embedding arrays  
- merge/split intermediates  
- validation buffers  

---

# What Each File Must Answer

| File | Answers These Questions |
|------|-------------------------|
| `40.20_master_program_guide.md` | What are the rules, workflow, philosophy, variable constraints, prototype structure, harness expectations, and AI Agent reporting standards? |
| `software_description.md` | What is this module for? Why does it exist? What are the requirements? What math/formatting/state does it use? What assumptions? |
| `prototype.py` | How is the module implemented? What is the public API? What state & local variables does it use? How is determinism preserved? |
| `harness.py` | How do we test this module? What evidence do we collect? How do we verify invariants, determinism, and parallel safety? |
| `verification_capsule.md` | Full Verification Capsule: migrated run records, evidence, failures, traceability, and architectural evaluation |
| `requirements_delta.md` | What requirements changed? Why? What new requirements emerged? |
| `../30_verification/30.30_verification_glossary.md` | Canonical shared vocabulary for scenario, artifact, deterministic_mode, provenance, and verification terms |

---

# AI Agent Execution & Reporting Standards

This section defines how the AI Agent (Copilot, Grok, etc.) must behave when executing code inside the playground.

## 1. Before Running

The AI Agent must read:

- `software_description.md`  
- `40.20_master_program_guide.md`  
- `verification_capsule.md`  
- `requirements_delta.md`  
- `../30_verification/30.30_verification_glossary.md`  

This ensures context, history, and requirement evolution are understood.

---

## 2. When Running Code

The AI Agent must:

- Run `prototype.py` or `harness.py`  
- Capture **full terminal output**  
- Record the **exact command used**  
- Capture all artifacts (JSON, logs, state dumps, etc.)

---

## 3. Required Output Structure

The AI Agent must reply using this template:

### Execution Report - [Module Name]

**Command:** `python harness.py`

**Result:** PASS / FAIL / PARTIAL  
**Exit Code:** 0 / n  

**Run Record Update:**
| Date | Module | Command | Inputs / Config | Result | Exit Code | Artifacts | HLR Ref | LLR Ref | Req Doc | Req Section | IO Fields Exercised | Negative-Path Coverage | Notes |
|------|--------|---------|------------------|--------|-----------|-----------|---------|---------|---------|-------------|---------------------|------------------------|-------|
| YYYY-MM-DD | ... | ... | ... | ... | ... | ... | HLR-2 | LLR-2.3 | software_description.md | Sec. 3.4.1 | ... | ... | ... |

**Key Observations**
- ...

**Insights & Learnings**
- ...

**Open Questions / Next Iteration**
- ...

**Suggested Next Steps**
1. ...
2. ...

---

# Test -> Requirement Attachment Rules

Every test run must explicitly attach itself to the requirement(s) it verifies.

This ensures full traceability:

> **High-Level Requirement (HLR)**  
-> **Low-Level Requirement (LLR)**  
-> **Test Case**  
-> **Evidence / Artifacts**  
-> **Verification Capsule**

### Required Columns in Run Record Table

| Column | Meaning |
|--------|---------|
| **HLR Ref** | High-level requirement ID (e.g., `HLR-2`) |
| **LLR Ref** | Low-level requirement ID (e.g., `LLR-2.3`) |
| **Req Doc** | Markdown file containing the requirement |
| **Req Section** | Section number inside the MD file |

### Rules for the AI Agent

- Every test must attach to at least one HLR and one LLR  
- If multiple requirements apply, list them comma-separated  
- If requirement is missing or unclear:  
  - Use `HLR-?` / `LLR-?`  
  - Document the gap in `verification_capsule.md` and `requirements_delta.md`  
- `harness.py` should emit requirement IDs during execution  
- `verification_capsule.md` must reference these IDs in the Verification Ledger  
- `verification_capsule.md` must record IO fields exercised and negative-path coverage  

---

# Verification Capsule

Every module must eventually produce a **Verification Capsule** (`verification_capsule.md`) that scientifically answers:

- What the module does and why it exists  
- Key invariants and responsibilities  
- How it was implemented and tested  
- Evidence of correctness and determinism  
- Performance and parallel-safety notes  
- Assumptions and open questions  
- Requirement IDs referenced in the Verification Ledger  
- IO fields exercised and negative-path coverage status  
- Links to artifact files in `artifacts/`  
- Architectural evaluation and structural recommendations  

This capsule is the **scientific record** of the module.

---

# Workflow

1. **Design** - Write/update `software_description.md`  
2. **Prototype** - Implement in `prototype.py` (macro-style)  
3. **Test & Verify** - Run `harness.py` and collect evidence  
4. **Attach Requirements** - Update run records with HLR/LLR references and IO fields exercised  
5. **Record** - Update `verification_capsule.md` and `requirements_delta.md`  
6. **Refine** - Update requirements, glossary usage, and traceability if needed  
7. **Stabilize** - When coherent and verified, promote to `50_thought_simulator_design/`  

When canonical TR anchors changed in `10_thought_simulator_req/10_system_architecture`, step 6 must explicitly include TR dirty-flag conformance checks against the updated 10.10 architecture set before any promotion step.

---

# Definition of Done (Structured Programming Gates)

A module iteration is considered done only when all gates below are satisfied.

## Required Gates

- Prototype remains macro-style and importable (no top-level execution).
- Harness is the sole execution entrypoint for verification evidence.
- Deterministic rerun check completed (at least 2 consecutive reruns with matching key identities/counters when deterministic mode is enabled).
- IO contract section in `software_description.md` is current and complete.
- Run Record row is added/updated with HLR, LLR, IO fields, and negative-path coverage fields.
- Verification Capsule is updated with scenario-level evidence and migration history.
- Requirements delta is updated for changed behavior.
- Failure review is captured in `verification_capsule.md` and `requirements_delta.md`.
- Evidence artifacts are produced in `artifacts/` and referenced by path.
- Open questions and assumptions recorded (or explicitly marked none).

## Minimum Evidence Bundle

- Terminal run output summary
- Artifact file(s)
- Updated run record row
- Updated verification capsule row(s)
- Updated requirements delta row(s)
- Updated glossary references if new terms are introduced

## Promotion Gate

Promotion to final design is allowed only when all Required Gates pass and no blocking determinism, traceability, or architecture issues remain unresolved.

---

# When a Module Graduates

A module leaves the playground when:

- Requirements feel stable and coherent  
- Prototype behaves correctly and deterministically  
- Harness + tests exist with clear expected outcomes  
- Verification Capsule is substantially complete  
- Requirements delta and glossary are complete  
- Major insights and failures are documented  

Only then is the polished version moved to the final design folder.

---

**Last Updated**: May 27, 2026  
**Version**: 0.5

---





