---
id: 20.105
version: "0.7"
date: 2026-06-13
status: requirements
source_of_truth: this
contains:
  - HLR: [HLR-20.105-001, HLR-20.105-002, HLR-20.105-003,
           HLR-20.105-004, HLR-20.105-005, HLR-20.105-006,
           HLR-20.105-007, HLR-20.105-008, HLR-20.105-009,
           HLR-20.105-010, HLR-20.105-011, HLR-20.105-012,
           HLR-20.105-013, HLR-20.105-014, HLR-20.105-015,
           HLR-20.105-016, HLR-20.105-017, HLR-20.105-018,
           HLR-20.105-019, HLR-20.105-020, HLR-20.105-021,
           HLR-20.105-022, HLR-20.105-023, HLR-20.105-024,
           HLR-20.105-025, HLR-20.105-026, HLR-20.105-027,
           HLR-20.105-028, HLR-20.105-029, HLR-20.105-030,
           HLR-20.105-031, HLR-20.105-032, HLR-20.105-033,
           HLR-20.105-034, HLR-20.105-035, HLR-20.105-036,
           HLR-20.105-037, HLR-20.105-038, HLR-20.105-039,
           HLR-20.105-040, HLR-20.105-041, HLR-20.105-042,
           HLR-20.105-043, HLR-20.105-044, HLR-20.105-045,
           HLR-20.105-046, HLR-20.105-047, HLR-20.105-048,
           HLR-20.105-049, HLR-20.105-050, HLR-20.105-051,
           HLR-20.105-052, HLR-20.105-053, HLR-20.105-054,
           HLR-20.105-055, HLR-20.105-056, HLR-20.105-057,
           HLR-20.105-058, HLR-20.105-059, HLR-20.105-060,
           HLR-20.105-061, HLR-20.105-062]
---

# 20.105 TP Requirements (v0.7 — Architecture-Aligned)

**Document ID:** 20.105
**Version:** 0.7
**Date:** 2026-06-13
**Status:** Draft v0.7 — Architecture-Aligned (Pipeline A Only)

## Purpose

Defines deterministic **Thought Packet (TP)** structure, lifecycle, field
authority, trajectory geometry persistence, and audit invariants for
**lane‑local Pipeline‑A operations**.

**Normative definition (HLR‑20.105‑046):** TP is the **minimal deterministic
state‑carrying artifact** and **primitive operand** of Pipeline A — the
per‑step, per‑lane working unit consumed and produced by each Pipeline‑A
stage, recording basin inputs/outputs, routing context, trajectory geometry,
and **append‑only historical evidence** (`lineage_log[]`, `tp_audit_log[]`)
without functioning as a second authoritative meaning store.

TP is the primitive operand consumed by OB, RB, TR, DCB, and Merge across all
Pipeline‑A stages. Authoritative meaning commits at Merge → TPU →
`semantic_core`.

**TB, IB, Truth/Done, and all Path‑B semantics are out of scope for Pipeline A
and SHALL NOT appear on TP.**

This module SHALL NOT weaken invariants in 20.12

TP includes evaluation‑derived metadata fields (TPTB, TPSF) only after truth and safety evaluation; these fields are immutable thereafter.

---

## Evaluation‑Derived Metadata Fields

Evaluation‑Derived Metadata Fields (TPTB, TPSF)
TPs MAY contain two evaluation‑derived metadata fields:

- TPTB (Truth Metadata) — produced exclusively by truth/completion evaluation (20.30.060) and defined structurally in 20.64_tptb_prim.md.
- TPSF (Safety Metadata) — produced exclusively by safety evaluation (20.170) and defined structurally in 20.66_tpsf_prim.md.

These fields are read‑only for all downstream components (Merge, TPU, RG) and SHALL NOT be present in lane‑local TPs prior to evaluation.

---

## Prohibited Pre‑Evaluation Fields

Lane‑local TPs SHALL NOT contain:
- TPTB
- TPSF
- any evaluation‑derived metadata
- any TPU‑only or MTP‑only fields

These fields SHALL be introduced only after their respective evaluation stages.

---

## Parent Scope

This module normatively decomposes TP obligations from parent documents,
including:

| Parent | Representative HLRs |
|--------|---------------------|
| [20.10](20.10_ts_architectural_principles.md) | HLR-20.010-073 (determinism), HLR-20.010-075 (safe-boundary enforcement), HLR-20.010-077 (bounded resources) |
| [20.12](20.12_ts_invariants.md) | HLR-20.012-001 (envelope partition), HLR-20.012-010 (seed boundary), HLR-20.012-011 (replay equivalence) |
| [20.30](20.30_ts_functional_model.md) | HLR-20.030-008 (pipeline determinism), HLR-20.030-009 (split/merge), HLR-20.030-304–310 (RB safe-boundary blocking) |
| [20.31](20.31_semantic_specification.md) | HLR-20.031-012 (TP pipeline operand), HLR-20.031-013 (forbidden TP fields) |

- **Pipeline:** Pipeline A only — TP SHALL NOT carry execution-layer or
  supervisory-envelope fields
- **Upstream:** RB lane assignment; Pipeline-A intake stages
  (InB, IIInB, CEx, CE, ISc)
- **Downstream:** Merge integrates lane TPs; TPU commits authoritative
  `semantic_core`
- **Bounds:** global overflow and TCU envelopes per 20.30 §8 and
  [20.90](20.90_ts_parameter_table.md)
- **Does not modify:** 20.10, 20.12, 20.20, 20.30 bootstrap documents

---

## Non‑Goals

- MTP authoritative aggregation — MTP (20.115)
- Split/merge algorithm detail — 20.130 and 20.131 (TP defines lineage obligations only; structural execution occurs in TE after RB arbitration).
- DCB event semantics — 20.106
- TR derivation algorithms — 20.37
- Final truth/done authority — 20.140 (`truth_hypotheses` / `done_state` authoritative on `semantic_core` post-merge)
- Pipeline B planning, expression, or IMR records on TP
- Final truth/done authority — 20.140 (truth/done commitment occurs only in semantic_core post‑merge; TP SHALL NOT store any truth/done fragments).

---

## 1. Dual‑Pipeline Boundary Discipline

### **Updated Truth/Done row**

| Concern | TP posture |
|---------|------------|
| `truth_hypotheses`, `done_state` | **SHALL NOT appear on TP**; authoritative commitment occurs only in `semantic_core` via Merge/`mtp_update`. No TB lane‑local fragments appear in Pipeline A. |
| `semantic_core` / MTP | TP **projects** meaning; authoritative commit at `mtp_update` only |
| `TP.TR`, `tr_needs_update` | Lane-local; TR routine exclusive `TP.TR` writer (20.37) |
| `exec_plan`, `exec_trace` | **SHALL NOT appear on TP** (20.31-013, 20.39-018) |
| `routing_epoch_id`, `opbeh_id`, `obg_id`, `xlater_id` | **SHALL NOT appear on TP** |
| `imr_record`, GB cap/mode records | **SHALL NOT appear on TP** — supervisory envelope only |
| `truth_hypotheses`, `done_state` | **Not authoritative on TP**; only TB lane-local pre-merge `channel_interpretations{}` / hypothesis fragments — no partial or provisional authoritative truth records (HLR-20.105-054) |
| DCB `directional_change_events` | **SHALL NOT be stored on TP** — ephemeral TR inputs only (20.106-014, HLR-20.105-053) |
| Seed | **SHALL NOT influence** TP field derivation (HLR-20.012-010) |

**Pipeline B forbidden:** OuB, OpBeh/OBG/XlateR, IMR, SRP SHALL NOT write TP fields or retroactively mutate lane-local Pipeline A projections.

---

## 2. TP as Lane‑Local Projection

TP SHALL represent the canonical per-step meaning-construction unit exchanged across deterministic Pipeline A boundaries (HLR-20.105-001).

| Store | Authority |
|-------|-----------|
| **MTP / `semantic_core`** | Authoritative cross-lane meaning, truth/done commitment, messy-input record |
| **TP** | Lane-local working projection: evidence, TR block, routing context, trajectory geometry, pre-merge interpretation fragments |

TP SHALL carry lane-specific views of propositions refs, ΔH%, suppression markers, routing context, and basin outputs (20.31-012). TP SHALL NOT duplicate authoritative `semantic_core` fields as a second source of truth.

Lane TPs merge into MTP per 20.30 §3.6; post-merge authoritative fields SHALL commit through Merge and `mtp_update`, not through stale TP copies.

### 2.1 Safe Boundary (Normative Dependency)

A **safe boundary** is a deterministic cycle point where supervisory annotations, finalization markers, split/merge commits, and GB-approved flow-control directives MAY be applied without interleaving active unsafeguarded basin mutations (20.30 HLR-20.030-304, 20.130-010).

For TP, safe boundaries include at minimum: post-Merge pre-`mtp_update`, RB blocked-relation enforcement points (20.30-304–310), and GB-approved TS→GB→TS annotation windows (HLR-20.105-009). Runtime invariants SHALL be enforced at every safe boundary per HLR-20.010-075.

TP `safe_boundary_marker` and `blocked_relation_log` entries SHALL be written only at safe boundaries — never mid-basin unsafeguarded mutation.

---

## 3. TP Field Model (Normative Minimum)

### 3.1 Identity block (immutable after creation)

```text
tp_identity {
  tp_id,
  tp_seq,                   // monotonic within sequence domain
  cycle_id,
  lane_id,
  parent_tp_id?,
  schema_version,           // TP wire/schema version
  profile_signature,        // bound runtime profile
  wire_map_version,         // serialization map version
  policy_signature,         // bound policy for replay
  timestamp_evidence?       // metadata only — NOT ordering authority
}
```

Identity block fields SHALL be immutable after TP creation (HLR‑20.105‑004, HLR‑20.105‑047).  
Versioning fields SHALL be present on every TP for deterministic replay validation.

---

### 3.2 Lineage log (append‑only, separate from identity)

```text
lineage_log[] {             // append-only; NOT part of identity block
  lineage_seq,
  module_id,
  event_type,               // CREATE | SPLIT | MERGE | UPDATE | RETIRE
  parent_ref?,
  child_refs[]?,            // canonical sort by stable ID
  partition_rationale_ref?,
  merge_contribution_ref?
}
```

Lineage extensions SHALL append only to `lineage_log[]` (HLR‑20.105‑048).  
Split/merge markers SHALL reference lineage entries and SHALL NOT mutate `tp_identity`.

---

### 3.3 Working projection fields

```text
tp_lane_projection {
  tp_identity,              // immutable block above
  lineage_log[],            // append-only

  // Thought Router (lane-local)
  TP.TR{},                  // TR routine exclusive writer
  tr_needs_update,          // init true; semantic writers set true; TR clears on success
  input_fields{},           // OB TR-input / evidence context
  routing_metadata{},       // RB routing context; not DCB derivation input

  // Trajectory geometry (DCB read set — §4)
  step_index,
  trajectory_position{},
  trajectory_direction{},
  trajectory_curvature{},

  // ΔH% lane fields (entropy trajectory — §5)
  delta_h{},                // Q32.32 contribution, missing-mass, step trajectory

  // Basin outputs (lane-local, pre-merge)
  evidence_fields[]?,       // OB; canonical sort by evidence_id
  basin_affiliation?,

  // Stability / overflow (lane-local markers)
  stability_markers{},
  overflow_metadata?,

  // Supervisory annotations (safe-boundary only)
  safe_boundary_marker?,
  blocked_relation_log[]?,  // append-only; §8.2 schema

  // Intake text fields (IIInB proposals; IE applies)
  intake_text,                // TP intake text after InB; not repaired until IE applies proposals
  intake_text_suggested?,     // IIInB-proposed repaired text; IE SHALL apply deterministically (HLR-20.105-063)
  intake_repair_trace[]?,     // IIInB repair proposals; IE SHALL apply deterministically (HLR-20.105-063)
  // IE applies all proposals deterministically per §3.5

  // Intake-bound repair (Track H — §3.4; IIInB-only writes)
  input_segments[]?,
  input_repair_tags[]?,
  iiinb_escalation_refs[]?,

  // Audit (append-only)
  tp_audit_log[]            // §8.1 ordering rules
}
```

---

### 3.4 Intake‑bound repair fields (Track H — Normative Minimum)

Per HLR‑20.105‑063, these fields record IIInB repair proposals only; IIInB SHALL NOT apply repairs directly. IE SHALL apply all proposed repairs deterministically when producing the final intake object. These proposals SHALL be reflected in TP.intake_text_suggested and TP.intake_repair_trace[] per HLR‑20.105‑064; TP.intake_text SHALL remain unrepaired until IE commit.

```text
input_segment {
  segment_ref,
  segment_span_start,
  segment_span_end,
  segment_class,             // SHORTHAND_ELIGIBLE | LITERAL | UNKNOWN
  iiinb_event_id
}

input_repair_tag {
  segment_ref,
  repair_outcome,            // APPLIED | ESCALATED | SKIPPED | TRUNCATED
  rule_id?,
  resolved_segment_ref?,
  usp_version_ref,
  iiinb_event_id
}

iiinb_escalation_ref {
  escalation_id,
  segment_ref,
  iiinb_event_id,
  escalation_reason_code
}
```

**Initialization:** new TP SHALL set `tr_needs_update = true` (20.37).  
TP creation SHALL initialize mandatory identity, core, and audit fields deterministically (HLR‑20.105‑013).

**Forbidden TP fields (HLR‑20.105‑032, ‑053, ‑054):**  
`routing_epoch_id`, `opbeh_id`, `obg_id`, `xlater_id`, `imr_record`, `gb_cap_record`,  
`gb_supervisory_mode`, `exec_plan`, `exec_trace`, DCB `directional_change_events`,  
**truth_hypotheses**, **done_state**, and **all truth/done fragments**.

No Path‑B fields SHALL appear on TP.

---

### 3.5 Intake Commit Semantics (IE)

Per HLR‑20.105‑063 and HLR‑20.105‑064, IE SHALL serve as the deterministic commit point for all IIInB-proposed repairs. IE SHALL apply the complete ordered proposal set recorded in TP.intake_text_suggested and TP.intake_repair_trace[] and SHALL produce the final repaired intake object (IE.intake_text). TP.intake_text SHALL remain unrepaired until IE commit.

---

## 4. Trajectory Geometry (DCB Cross‑Reference)

TP SHALL persist **geometric** trajectory fields consumed by DCB (HLR‑20.105‑027, 20.106 §3) —  
distinct from ΔH% entropy trajectory fields in §5.

| Field | Semantics |
|-------|-----------|
| `trajectory_position` | TP coordinate in trajectory space at `step_index` |
| `trajectory_direction` | Geometric tangent at observation step |
| `trajectory_curvature` | Rate of change of direction vector along trajectory |
| `step_index` | Deterministic step ordinal within lane/cycle context |

Geometry fields SHALL be updated during deterministic Pipeline‑A cycle transitions.  
Numeric encoding SHALL follow 20.95 (Q32.32 canonical).

**Writers:** Pipeline‑A trajectory persistence (deterministic step advance).  
**Readers:** DCB (read‑only).  
OB, TR, DCB, and Pipeline B SHALL NOT write trajectory geometry fields.

Multi‑step DCB observation SHALL use consecutive persisted TP instances.  
MTP trajectory‑projection stitching fields are defined in 20.106.

TP SHALL NOT persist DCB directional‑change events; events are ephemeral TR inputs  
recomputed from geometry on replay (20.106‑014, HLR‑20.105‑053).

---

### 4.1 Geometric vs entropy trajectory (comparison)

| Aspect | Geometric trajectory (§4) | Entropy trajectory — ΔH% (§5) |
|--------|---------------------------|-------------------------------|
| **Purpose** | DCB curvature/direction observation | Missing‑mass and entropy accounting |
| **Primary consumer** | DCB (read‑only) | OB, Merge, regulators (via MTP) |
| **TP fields** | `trajectory_position`, `trajectory_direction`, `trajectory_curvature`, `step_index` | `delta_h.contribution_q32`, `missing_mass_q32`, `step_delta_q32`, `cumulative_q32?` |
| **Numeric type** | Q32.32 canonical | Q32.32 canonical |
| **Writers** | Pipeline‑A trajectory persistence | OB / basin modules / Merge |
| **Replay** | Recomputable from consecutive TP geometry | Recomputable from lane evidence + policy |
| **Cross‑ref** | 20.106 | 20.31, 20.40, 20.90 |

---

## 5. ΔH% Lane Fields

## 5. ΔH% Lane Fields (Entropy Trajectory)

TP `delta_h{}` SHALL carry lane-local **entropy trajectory** fields (HLR-20.105-051) — not DCB geometric trajectory (§4):

```text
delta_h {
  contribution_q32,       // lane ΔH% contribution this step (Q32.32)
  missing_mass_q32,         // unresolved missing mass (Q32.32)
  step_delta_q32,           // per-cycle step contribution
  cumulative_q32?           // optional bounded cumulative marker
}
```

Semantics defer to [20.31](20.31_semantic_specification.md) (ΔH% / missing-mass), OB contribution rules ([20.40](20.40_ob_requirements.md)), Merge reconciliation ([20.130](20.130_splitting_and_merging_requirements.md)), and limit enforcement ([20.90](20.90_ts_parameter_table.md) §B). Numeric fields SHALL use canonical Q32.32 per 20.95.

---

## 6. Writer Authority Matrix (Updated)

### **Remove TB and IB entirely**

Updated table:

| Field group | Writers | Notes |
|-------------|---------|-------|
| `tp_identity` | TP creation only | Immutable |
| `lineage_log[]` | Split/Merge/RB/audit layer | Append-only |
| `TP.TR` | TR routine only | TR clears `tr_needs_update` |
| `tr_needs_update` | OB, Merge, ΔH% logic, messy-input classifiers | IB removed |
| `input_fields`, `evidence_fields` | OB | |
| `routing_metadata` | RB | |
| `trajectory_*`, `step_index` | Pipeline A trajectory persistence | |
| `delta_h`, `stability_markers`, `overflow_metadata` | Basin modules / Merge | |
| `blocked_relation_log`, `safe_boundary_marker` | GB-approved safe-boundary pathways | |
| `input_segments[]`, `input_repair_tags[]`, `iiinb_escalation_refs[]` | IIInB only | |
| `tp_audit_log` | TS cycle audit layer | Append-only |

Remove:

- `channel_interpretations` row  
- IB from tr_needs_update writers  

---

## 7. Identity, Sequence, and Lifecycle

- Every TP SHALL include stable identity fields per §3.1, including `schema_version`, `profile_signature`, and `wire_map_version` (HLR-20.105-002, HLR-20.105-047)
- `tp_seq` SHALL be monotonic within the configured deterministic sequence domain and SHALL preserve FIFO interpretation order (HLR-20.105-003)
- `tp_identity` fields SHALL be immutable after creation; lineage continuity SHALL extend only via append-only `lineage_log[]` (HLR-20.105-004, HLR-20.105-048)
- Timestamps SHALL be evidence metadata only; ordering authority SHALL derive from `tp_seq`, `cycle_id`, and canonical sequence tokens — **not** wall-clock time (HLR-20.105-018)
- TP SHALL NOT permit nondeterministic mutation of finalized projection fields once `safe_boundary_marker.finalized = true` (HLR-20.105-014)
- Lifecycle transitions SHALL be logged in append-only `tp_audit_log` (HLR-20.105-024)

**COB promotion (HLR-20.105-015):** TP promotion to COB-visible conversation-object semantics SHALL occur only through approved deterministic lifecycle pathways per [20.31](20.31_semantic_specification.md) §7 (COB) and 20.30 HLR-20.030-293. Minimum preconditions: valid `tp_identity`, complete `lineage_log[]`, Merge-committed `semantic_core` snapshot ref, and `policy_signature` binding. Promotion is **not reversible** at TP layer — deprecation SHALL follow COB retirement semantics only.

**Creation-order independence (HLR-20.105-040):** TP field derivation and basin outputs SHALL NOT depend on TP allocation order, heap/container address identity, or creation sequence except through explicit canonical fields (`tp_id`, `tp_seq`, `lane_id`).

---

## 8. Split / Merge Lineage

TP merge/split lineage markers SHALL preserve deterministic parent/child provenance continuity (HLR-20.105-017).

Split-derived lane TPs SHALL retain `parent_tp_id` and append `lineage_log` entries with `event_type = SPLIT` and `partition_rationale_ref` (20.130). Merge-target TPs SHALL append `event_type = MERGE` entries with `merge_contribution_ref`.

Split and merge operations SHALL NOT introduce inference, hallucinated reconstruction, or meaning invention beyond explicit source fields (20.130-006). Detailed split/merge algorithms are normative in 20.130; TP obligations here are lineage and audit preservation only.

---

## 9. Audit, Supervisory Annotations, Retention, and Reject Records

### 9.1 Audit log ordering (HLR-20.105-049)

`tp_audit_log[]` entries SHALL sort before export/hash/replay diff by composite key, in precedence order:

1. `cycle_id` (ascending)
2. `tp_seq` (ascending)
3. `audit_seq` (ascending, monotonic per TP)
4. `reason_code` (lexicographic tie-break)

`timestamp_evidence`, when present, SHALL NOT override this ordering.

### 9.2 Blocked-relation log schema (HLR-20.105-008)

```text
blocked_relation_entry {
  block_id,                 // stable deterministic ID
  obx_class,
  oby_class,
  rbxy_id,
  reason_code,              // immutable dictionary ID
  policy_signature_ref,
  cycle_id,
  tp_seq,
  audit_seq,
  safe_boundary_marker_ref
}
```

`blocked_relation_log[]` SHALL sort by `(cycle_id, tp_seq, audit_seq, reason_code)` before export/hash (HLR-20.105-049). Reason codes SHALL map through immutable versioned dictionaries (HLR-20.105-012).

### 9.3 Reject-with-audit schema (HLR-20.105-050)

When TP rejects unsupported schema, enum, profile, or wire-map states (HLR-20.105-019):

```text
tp_reject_record {
  reject_id,
  reject_class,             // SCHEMA | ENUM | PROFILE | WIRE_MAP
  reason_code,              // deterministic dictionary ID
  observed_schema_version?,
  observed_profile_signature?,
  observed_wire_map_version?,
  policy_signature_ref,
  cycle_id,
  tp_seq,
  evidence_fields{}         // minimal observed-state snapshot
}
```

Reject records SHALL append to `tp_audit_log` with `event_type = REJECT` and SHALL NOT partially mutate `tp_identity` or finalized projection fields.

### 9.4 Zero-event windows (HLR-20.105-052)

When a cycle produces **no basin mutations** for a lane (zero-event window):

- TS MAY emit a **synthetic continuity TP** with `event_type = ZERO_EVENT` in `tp_audit_log`
- `tp_seq` SHALL still increment monotonically for the lane sequence domain
- No new `evidence_fields[]` or basin outputs are required
- `tp_audit_log` SHALL carry fixed reason code `TP_ZERO_EVENT_WINDOW` or canonical empty-evidence marker per policy
- Zero-event windows SHALL NOT skip `tp_seq` values or reorder FIFO interpretation order

### 9.5 Retention and replay horizon

Retention and compaction SHALL be bounded by deterministic policy and **replay-horizon** semantics per [20.115](20.115_mtp_requirements.md) HLR-20.115-018 and 20.30 functional retention rules (HLR-20.105-016). Replay horizon is **profile-bound** via `profile_signature` — not wall-clock derived.

- TP SHALL maintain append-only audit invariants; existing audit entries SHALL NOT be overwritten or deleted in-place (HLR-20.105-010)
- Supervisory annotations on TP SHALL occur only through approved deterministic TS→GB→TS pathways at safe boundaries (HLR-20.105-009)

---

## 10. Canonical Ordering and Serialization

**Canonical ordering (HLR-20.105-038):** `lineage_log[]`, `evidence_fields[]`, nested ID arrays, `tp_audit_log[]`, and `blocked_relation_log[]` SHALL sort per §9.1/§9.2 before export, hash, and replay diff.

`TP.TR` nested maps SHALL sort per [20.37](20.37_thought_router_tr_specification.md) HLR-20.037-064: stable `hint_id` lexicographic primary sort; nested ID arrays lexicographic; numeric weights canonical Q32.32. `routing_metadata` nested arrays SHALL use stable-ID lexicographic sort.

TP export manifests SHALL use deterministic key ordering and canonical serialization (HLR-20.105-023).

Numeric TP fields SHALL use canonical Q32.32; −0→+0 before comparison and export (HLR-20.105-034).

---

## 11. Determinism and Replay

- TP core-field updates SHALL be deterministic, schema-versioned, auditable, and reproducible for identical effective state (HLR-20.105-006)
- TP SHALL preserve platform-independent deterministic behavior under equivalent input, `profile_signature`, and `policy_signature` state (HLR-20.105-020)
- TP field outputs SHALL reproduce from `(lane input, MTP projection, policy_signature)` — **seed excluded** (HLR-20.105-035)
- Strip replay removing Pipeline B envelopes SHALL preserve TP-merged meaning in `semantic_core` (20.36 Class 1, HLR-20.105-036)
- TP processing SHALL remain bounded (HLR-20.105-021)
- Unsupported schema/profile/wire-map states SHALL emit `tp_reject_record` per §9.3 — never silent accept (HLR-20.105-019, HLR-20.105-050)

---

## Worked Example — Lane TP Through Pipeline A (Updated)

Remove TB step:

| Step | Actor | TP state change |
|------|--------|-----------------|
| 1 | Creation | `tp_identity` + `tr_needs_update = true`; `lineage_log` CREATE entry |
| 2 | RB | Assigns `lane_id`; writes `routing_metadata` |
| 3 | OB | Writes `evidence_fields[]`, `input_fields`, `delta_h.contribution_q32` |
| 4 | Trajectory persistence | Updates `step_index`, geometry fields |
| 5 | DCB | Reads geometry only — **no TP writes** |
| 6 | TR | Writes `TP.TR`; sets `tr_needs_update = false` |
| 7 | Merge | Integrates lane TPs → MTP; `mtp_update` commits `semantic_core` |
| 8 | Strip replay | Pipeline B removed → merged `semantic_core` stable |

---

## Normative Requirements

1. HLR‑20.105‑001: TP SHALL represent the canonical per‑step meaning‑construction unit exchanged across deterministic Pipeline A boundaries.
2. HLR‑20.105‑002: Every TP SHALL include stable identity fields including `tp_id`, `tp_seq`, `cycle_id`, `lane_id`, `schema_version`, `profile_signature`, `wire_map_version`, `policy_signature`, lineage identifiers, and parent pointers.
3. HLR‑20.105‑003: TP `tp_seq` SHALL be monotonic within the configured deterministic sequence domain and SHALL preserve FIFO interpretation order.
4. HLR‑20.105‑004: TP `tp_identity` block fields SHALL be immutable after creation; lineage continuity SHALL extend only through append‑only `lineage_log[]` entries.
5. HLR‑20.105‑005: TP core fields SHALL include lane‑local OB outputs, RB routing context, ΔH% lane fields (`delta_h{}`), geometric trajectory fields (§4), and bounded stability markers.
6. HLR‑20.105‑006: TP core‑field updates SHALL be deterministic, schema‑versioned, auditable, and reproducible for identical effective state.
7. HLR‑20.105‑007: TP supervisory annotation fields SHALL include safe‑boundary markers, policy‑enforcement markers, and blocked‑relation logs — not full GB supervisory envelope records.
8. HLR‑20.105‑008: TP `blocked_relation_log` entries SHALL conform to §9.2 minimum schema and SHALL record OBx class, OBy class, RBxy identifier, policy reason code, and `tp_seq`/`cycle_id` evidence.
9. HLR‑20.105‑009: TP supervisory annotations SHALL occur only through approved deterministic TS→GB→TS supervisory pathways at safe boundaries per §2.1.
10. HLR‑20.105‑010: TP SHALL maintain append‑only audit invariants; existing audit entries SHALL NOT be overwritten or deleted in‑place.
11. HLR‑20.105‑011: TP audit records SHALL be deterministic, bounded, replayable, and serialized with canonical field ordering per §9.1.
12. HLR‑20.105‑012: TP reason codes SHALL use immutable identifiers with deterministic versioned dictionary mapping behavior.
13. HLR‑20.105‑013: TP creation SHALL initialize all mandatory identity, core, and audit fields with deterministic defaults; new TP SHALL initialize `tr_needs_update = true`.
14. HLR‑20.105‑014: TP SHALL NOT permit nondeterministic mutation of previously finalized projection fields once `safe_boundary_marker.finalized = true`.
15. HLR‑20.105‑015: TP promotion to COB‑visible conversation‑object semantics SHALL occur only through approved deterministic lifecycle pathways per 20.31 §7 and 20.30‑293; promotion requires Merge‑committed `semantic_core` ref and is not reversible at TP layer.
16. HLR‑20.105‑016: TP retention and compaction behavior SHALL be bounded by deterministic policy and profile‑bound replay‑horizon semantics per 20.115‑018.
17. HLR‑20.105‑017: TP merge/split lineage markers SHALL preserve deterministic parent/child provenance continuity via `lineage_log[]` per 20.130.
18. HLR‑20.105‑018: TP timestamps SHALL be treated as evidence metadata only; ordering authority SHALL derive from deterministic sequence tokens (`tp_seq`, `cycle_id`, `audit_seq`).
19. HLR‑20.105‑019: TP SHALL reject unsupported schema, enum, profile, or wire‑map states with deterministic reject‑with‑audit behavior per §9.3.
20. HLR‑20.105‑020: TP SHALL preserve platform‑independent deterministic behavior under equivalent input, `profile_signature`, and `policy_signature` state.
21. HLR‑20.105‑021: TP processing SHALL remain bounded and SHALL NOT require unbounded scanning or inference to validate invariants.
22. HLR‑20.105‑022: TP zero‑event windows SHALL follow §9.4 semantics; `tp_seq` SHALL still advance monotonically.
23. HLR‑20.105‑023: TP export manifests SHALL use deterministic key ordering and canonical serialization.
24. HLR‑20.105‑024: TP lifecycle transitions SHALL be logged in append‑only audit records with deterministic when/why/how context.
25. HLR‑20.105‑025: TP requirements in this module SHALL satisfy and SHALL NOT weaken parent invariants defined by 20.10, 20.12, and 20.30.
26. HLR‑20.105‑026: TP invariants SHALL preserve determinism, reproducibility, boundedness, and auditability for all supported runtime profiles.
27. HLR‑20.105‑027: TP SHALL persist geometric trajectory fields (`trajectory_position`, `trajectory_direction`, `trajectory_curvature`, `step_index`) consumed by DCB per 20.106 §3.
28. HLR‑20.105‑028: TP SHALL be a lane‑local projection of MTP / `semantic_core` and SHALL NOT function as a second authoritative meaning store (20.31‑012).
29. HLR‑20.105‑029: TP SHALL operate exclusively in Pipeline A; execution‑layer and supervisory‑envelope fields SHALL NOT be stored on TP instances.
30. HLR‑20.105‑030: Only the TR routine SHALL write `TP.TR`; RB, OB, DCB, Merge, and Pipeline B SHALL NOT write `TP.TR`.
31. HLR‑20.105‑031: Semantic‑writer modules that modify semantic‑relevant TP fields SHALL set `tr_needs_update = true`; the TR routine SHALL clear `tr_needs_update = false` only after successful TR recompute and commit.
32. HLR‑20.105‑032: TP SHALL NOT carry `routing_epoch_id`, `opbeh_id`, `obg_id`, `xlater_id`, `imr_record`, GB cap/mode records, `exec_plan` refs, or `exec_trace` refs.
33. HLR‑20.105‑033: Authoritative `truth_hypotheses` and `done_state` SHALL NOT be stored on TP; authoritative commitment SHALL occur only in `semantic_core` via Merge/`mtp_update`.
34. HLR‑20.105‑034: Authoritative numeric TP fields SHALL use canonical Q32.32 per 20.95; −0→+0 before comparison and export.
35. HLR‑20.105‑035: TP field derivation SHALL be reproducible from `(lane input, MTP projection, policy_signature)`; seed SHALL NOT influence TP meaning fields.
36. HLR‑20.105‑036: Strip replay removing Pipeline B envelopes SHALL preserve TP‑merged meaning in `semantic_core` for Pipeline‑A‑only equivalence (20.36 Class 1).
37. HLR‑20.105‑037: DCB, OB, TR, and Pipeline B modules SHALL NOT write TP geometric trajectory fields.
38. HLR‑20.105‑038: TP arrays and nested maps listed in §10 SHALL use deterministic canonical ordering before export, hash, and replay diff per 20.95/20.39.
39. HLR‑20.105‑039: `routing_metadata` on TP SHALL be RB routing context only; DCB and Pipeline B SHALL NOT write routing metadata.
40. HLR‑20.105‑040: TP field derivation and basin outputs SHALL NOT depend on TP allocation order, heap/container identity, or creation sequence except through explicit canonical fields.
41. HLR‑20.105‑041: TP SHALL NOT contain nondeterministic or platform‑dependent floating‑point representations; all numeric fields SHALL follow 20.95.
42. HLR‑20.105‑042: TP SHALL NOT contain unbounded arrays or maps; all TP structures SHALL be bounded by deterministic policy.
43. HLR‑20.105‑043: TP SHALL NOT contain stale or orphaned lineage references; lineage continuity SHALL be validated at safe boundaries.
44. HLR‑20.105‑044: TP SHALL NOT contain partial or provisional supervisory annotations; all annotations SHALL occur only at safe boundaries.
45. HLR‑20.105‑045: TP SHALL NOT contain partial or provisional basin outputs; OB outputs SHALL be complete for the cycle.
46. HLR‑20.105‑046: TP SHALL serve as the minimal deterministic state‑carrying artifact exchanged across Pipeline A boundaries.
47. HLR‑20.105‑047: TP identity fields SHALL remain stable across replay and SHALL NOT be regenerated.
48. HLR‑20.105‑048: TP lineage entries SHALL be append‑only and SHALL NOT be mutated or reordered.
49. HLR‑20.105‑049: TP audit entries SHALL sort deterministically per §9.1 before export/hash/replay diff.
50. HLR‑20.105‑050: TP SHALL emit deterministic reject‑with‑audit records for unsupported schema/profile/wire‑map states.
51. HLR‑20.105‑051: TP SHALL persist ΔH% entropy trajectory fields per §5.
52. HLR‑20.105‑052: TP zero‑event windows SHALL follow §9.4 semantics.
53. HLR‑20.105‑053: TP SHALL NOT persist DCB directional‑change events; such events SHALL be recomputed from geometry on replay.
54. HLR‑20.105‑054: TP SHALL NOT store any truth/done fragments; all truth/done commitment occurs only in `semantic_core`.
55. HLR‑20.105‑055: TP SHALL NOT contain any Path‑B fields or outputs.
56. HLR‑20.105‑056: TP SHALL NOT contain semantic‑repair fields or MI_* categories.
57. HLR‑20.105‑057: TP SHALL NOT contain any fields written by IB or TB.
58. HLR‑20.105‑058: TP SHALL NOT contain any fields requiring post‑TS interrogation.
59. HLR‑20.105‑059: TP SHALL NOT contain any fields requiring non‑deterministic inference.
60. HLR‑20.105‑060: TP SHALL remain strictly Pipeline‑A‑only; no Path‑B semantics SHALL appear on TP.
61. HLR‑20.105‑061 — Truth Metadata Field
TP SHALL include a truth‑metadata field (TPTB) when produced by truth/completion evaluation. This field SHALL be read‑only for all components except truth evaluation and SHALL conform to 20.64_tptb_prim.md.
62. HLR‑20.105‑062 — Safety Metadata Field
TP SHALL include a safety‑metadata field (TPSF) when produced by safety evaluation. This field SHALL be read‑only for all components except safety evaluation and SHALL conform to 20.66_tpsf_prim.md.
63. HLR‑20.105‑063: Intake‑bound repair fields SHALL record IIInB repair proposals only; IIInB SHALL NOT apply repairs directly. IE SHALL apply all proposed repairs deterministically when producing the final intake object.
64. HLR‑20.105‑064: TP.intake_text_suggested SHALL contain IIInB-proposed repairs only; TP.intake_text SHALL remain unrepaired until IE applies all proposals deterministically. TP.intake_repair_trace[] SHALL record the complete ordered proposal set for IE commit.

---

## Worked Example — Lane TP Through Pipeline A (Updated)

| Step | Actor | TP state change |
|------|--------|-----------------|
| 1 | Creation | `tp_identity` + `tr_needs_update = true`; `lineage_log` CREATE entry |
| 2 | RB | Assigns `lane_id`; writes `routing_metadata` |
| 3 | OB | Writes `evidence_fields[]`, `input_fields`, `delta_h.contribution_q32` |
| 4 | Trajectory persistence | Updates `step_index`, geometry fields |
| 5 | DCB | Reads geometry only — **no TP writes** |
| 6 | TR | Writes `TP.TR`; sets `tr_needs_update = false` |
| 7 | Merge | Integrates lane TPs → MTP; `mtp_update` commits `semantic_core` |
| 8 | Strip replay | Pipeline B removed → merged `semantic_core` stable |

---
