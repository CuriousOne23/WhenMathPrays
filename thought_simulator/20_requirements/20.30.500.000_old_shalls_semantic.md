<details>
<summary>HLR‑20.030‑001 → 015</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | TS core execution SHALL be deterministic for identical input, MTP state, and seed. | Core determinism invariant; same input/state → same output. |
| 002 | Meaning representation and construction SHALL remain explicit, structured, and auditable via MTP-centered semantics. | Meaning must be explicit, structured, and stored in the MTP. |
| 003 | TS architecture SHALL remain modular and inspectable across OB, RB, TB, and TS-core primitives. | Architecture must be modular, separable, and testable. |
| 004 | Meaning construction SHALL combine path semantics, ΔH% contribution/missing-mass accounting, and TB interpretation deterministically. | Meaning = OB/RB path + ΔH% + TB interpretation. |
| 005 | Variability SHALL be isolated from TS core cognition and SHALL NOT introduce nondeterminism into OB/RB/TB/MTP logic. | Variability allowed only outside core cognition; core stays deterministic. |
| 006 | Random-seed handling SHALL remain isolated, documented, reproducible, and deterministic for fixed input/state/seed tuples. | Random seed must be isolated and reproducible; no effect on meaning. |
| 007 | Primitive contracts for TP, MTP, OB, RB, TB, ΔH%, and TCU SHALL remain explicit and stable. | All primitive interfaces must be explicit and stable across versions. |
| 008 | End-to-end pipeline stages (input, routing, splitting, OB processing, TB interpretation, merging, truth/done, response) SHALL be deterministic. | Every pipeline stage must be deterministic. |
| 009 | Splitting and merging SHALL be deterministic, auditable, and loss-accounted through ΔH% semantics. | Splitting/merging must be deterministic and track information loss via ΔH%. |
| 010 | Operational semantics and MTP update mapping SHALL remain explicit and reproducible. | MTP update rules must be explicit and reproducible. |
| 011 | TCU budgeting, degradation policy, and per-tick cost reporting SHALL be explicit, bounded, and deterministic. | TCU usage must be bounded and deterministic; degradation must follow rules. |
| 012 | Randomness subsystem requirements SHALL enforce isolation, reproducibility, and safety-gated change control. | Randomness subsystem must be isolated and controlled. |
| 013 | Noisy or ill-formed input handling SHALL remain deterministic and SHALL preserve stable/auditable meaning construction. | Even malformed input must be processed deterministically. |
| 014 | TS architectural behavior SHALL remain interpretable and diagnostically visible across all lifecycle stages. | System must be interpretable and diagnosable at all times. |
| 015 | ΔH% SHALL be defined as the normalized structural-entropy delta… deterministic, monotonic, MI-class-normalized, and SHALL NOT encode probability, truth, or semantic inference. | Canonical ΔH% definition; deterministic entropy delta, not probability. |

</details>

<details>
<summary>HLR‑20.030‑001 → 015</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | TS core execution SHALL be deterministic for identical input, MTP state, and seed. | Core determinism invariant; same input/state → same output. |
| 002 | Meaning representation and construction SHALL remain explicit, structured, and auditable via MTP-centered semantics. | Meaning must be explicit, structured, and stored in the MTP. |
| 003 | TS architecture SHALL remain modular and inspectable across OB, RB, TB, and TS-core primitives. | Architecture must be modular, separable, and testable. |
| 004 | Meaning construction SHALL combine path semantics, ΔH% contribution/missing-mass accounting, and TB interpretation deterministically. | Meaning = OB/RB path + ΔH% + TB interpretation. |
| 005 | Variability SHALL be isolated from TS core cognition and SHALL NOT introduce nondeterminism into OB/RB/TB/MTP logic. | Variability allowed only outside core cognition; core stays deterministic. |
| 006 | Random-seed handling SHALL remain isolated, documented, reproducible, and deterministic for fixed input/state/seed tuples. | Random seed must be isolated and reproducible; no effect on meaning. |
| 007 | Primitive contracts for TP, MTP, OB, RB, TB, ΔH%, and TCU SHALL remain explicit and stable. | All primitive interfaces must be explicit and stable across versions. |
| 008 | End-to-end pipeline stages (input, routing, splitting, OB processing, TB interpretation, merging, truth/done, response) SHALL be deterministic. | Every pipeline stage must be deterministic. |
| 009 | Splitting and merging SHALL be deterministic, auditable, and loss-accounted through ΔH% semantics. | Splitting/merging must be deterministic and track information loss via ΔH%. |
| 010 | Operational semantics and MTP update mapping SHALL remain explicit and reproducible. | MTP update rules must be explicit and reproducible. |
| 011 | TCU budgeting, degradation policy, and per-tick cost reporting SHALL be explicit, bounded, and deterministic. | TCU usage must be bounded and deterministic; degradation must follow rules. |
| 012 | Randomness subsystem requirements SHALL enforce isolation, reproducibility, and safety-gated change control. | Randomness subsystem must be isolated and controlled. |
| 013 | Noisy or ill-formed input handling SHALL remain deterministic and SHALL preserve stable/auditable meaning construction. | Even malformed input must be processed deterministically. |
| 014 | TS architectural behavior SHALL remain interpretable and diagnostically visible across all lifecycle stages. | System must be interpretable and diagnosable at all times. |
| 015 | ΔH% SHALL be defined as the normalized structural-entropy delta… deterministic, monotonic, MI-class-normalized, and SHALL NOT encode probability, truth, or semantic inference. | Canonical ΔH% definition; deterministic entropy delta, not probability. |

</details>
<details>
<summary>HLR‑20.030‑221 → 240</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 221 | GB SHALL NOT read OB, RB, TB, IB, InB, or OuB internal state or MTP internal state directly. | GB is isolated from internal basin/MTP state; can only read lane-local TP + MPs. |
| 222 | GB SHALL operate within an explicit small TS cycle share (nominally 3–5%) and a bounded TCU envelope, and SHALL degrade/fallback deterministically when exceeded. | GB has strict compute/TCU limits and must degrade deterministically. |
| 223 | TS service requests that affect global behavior… SHALL route through GB for deterministic stability/drift evaluation. | All global behavior changes must be evaluated by GB. |
| 224 | GB MAY issue bounded deterministic supervisory flow-control actions… and these actions SHALL NOT mutate TP/MTP meaning-construction state. | GB can influence flow but cannot change meaning. |
| 225 | GB SHALL expose a deterministic supervisory command interface for higher-level safety/supervisory controllers. | GB must provide a stable, deterministic external API. |
| 226 | GB SHALL translate external supervisory commands into bounded TS-level supervisory actions consistent with the global stability envelope. | External commands must be converted into safe, bounded TS actions. |
| 227 | TS<->GB and external<->GB supervisory interfaces SHALL be bounded, deterministic, measurable, and constrainable by directionality, mechanism, timing, and rate/priority limits. | All supervisory interfaces must be deterministic and rate/priority constrained. |
| 228 | Detailed GB mode definitions, message schemas, priority models, and concrete rate limits SHALL be specified in 20.80 GB Requirements and traced to this subsection. | GB behavior must be fully specified and traceable. |
| 229 | TS core SHALL execute OB, RB, TB, and MTP logic deterministically and SHALL NOT perform global supervisory decisions. | TS core handles meaning; GB handles supervision. |
| 230 | TS core SHALL escalate global decisions… to GB. | TS core must escalate global decisions to GB. |
| 231 | GB SHALL evaluate and approve or deny global supervisory actions… | GB is the approval authority for global actions. |
| 232 | GB SHALL operate independently of TS execution and MAY run on a separate processor or safety controller. | GB may run on separate hardware. |
| 233 | When OuB tags incomplete meaning, TS core SHALL generate an IB-Creation-Request and route it to GB for deterministic evaluation. | IB creation requests originate from OuB-tagged incomplete meaning. |
| 234 | TS core SHALL NOT instantiate an IB without GB approval. | IB creation requires GB authorization. |
| 235 | GB SHALL issue deterministic supervisory actions… that affect TS flow and SHALL NOT mutate TP/MTP meaning-construction state. | GB influences flow only; cannot change meaning. |
| 236 | GB MAY be implemented on a coprocessor or safety controller; TS core SHALL communicate with GB via a deterministic supervisory interface… | GB may be external; communication must be deterministic. |
| 237 | GB SHALL remain isolated from OB, RB, and TB internal state and SHALL read only TP lane-local state and MPs. | Reinforces GB’s read-only boundaries. |
| 238 | TS core SHALL remain isolated from GB internal supervisory logic. | TS cannot read GB internals. |
| 239 | TS SHALL send IB-Creation-Requests to GB and SHALL continue execution without waiting for immediate GB response. | IB creation workflow is asynchronous. |
| 240 | GB SHALL evaluate inquiry requests asynchronously and SHALL issue supervisory interrupts when decisions are ready. | GB responds asynchronously via interrupts. |

</details>
<details>
<summary>HLR‑20.030‑241 → 260</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 241 | TS SHALL store OuB‑tagged TP/MTP snapshots in a bounded pending inquiry buffer. | OuB‑tagged incomplete meaning snapshots must be stored in a bounded buffer. |
| 242 | If GB denies an inquiry request, TS SHALL deterministically discard the corresponding pending inquiry buffer entry. | Denied inquiries must be deterministically removed. |
| 243 | If GB approves an inquiry request, TS SHALL instantiate the IB using the stored pending snapshot. | Approved inquiries become IBs instantiated from the stored snapshot. |
| 244 | After GB approval, TS SHALL update RB routing to enable OB<->IB communication. | RB routing must be updated to allow OB–IB communication. |
| 245 | OuB SHALL NOT route directly to IB at any time. | OuB cannot communicate directly with IB. |
| 246 | IB SHALL receive evidence from OB and SHALL send inquiry requests to OB. | IB participates in OB evidence flow and can request deeper inquiry. |
| 247 | GB SHALL supervise IB evolution continuously and MAY issue bounded supervisory actions including Crop, Prune, Dampen, Slow, Suspend, Reshape, and Kill. | GB supervises IB behavior and can apply bounded corrective actions. |
| 248 | TS SHALL apply GB supervisory actions only at deterministic safe boundaries. | Supervisory actions must occur only at deterministic safe boundaries. |
| 249 | GB SHALL evaluate IBs for promotion using deterministic criteria including stability, ΔH% reduction, usage frequency, conflict rate, and TCU efficiency. | IB promotion must follow deterministic evaluation criteria. |
| 250 | TS SHALL collapse IB->OB only after GB approval. | IB promotion to OB requires GB approval. |
| 251 | TS SHALL update OB registry state and RB routing deterministically after approved IB->OB promotion. | OB registry and routing must be updated deterministically after promotion. |
| 252 | GB SHALL evaluate OBs for decomposition under excessive complexity, semantic sprawl, or degraded ΔH% profiles. | GB can request OB decomposition based on deterministic criteria. |
| 253 | GB MAY request IB‑based decomposition of an OB into sub‑OBs. | OB decomposition may be performed via IBs. |
| 254 | TS SHALL instantiate decomposition IBs and SHALL retire the original OB only after GB approval. | Decomposition requires GB approval and produces new IBs. |
| 255 | TS and GB SHALL log split, merge, IB creation, IB evolution, IB promotion, OB decomposition, and supervisory actions with when, why, and how context. | All supervisory lifecycle events must be logged with full context. |
| 256 | Supervisory lifecycle logs SHALL be accessible for audit and version control. | Logs must be accessible for auditing and version control. |
| 257 | Upon GB approval, TS SHALL initialize IB metadata fields for identity, origin, evolution, inquiry state, promotion, and decomposition/retirement state. | IB metadata must be initialized deterministically. |
| 258 | TS SHALL populate IB identity and origin metadata from the pending inquiry buffer and supervisory request context. | IB identity/origin come from the pending buffer and GB request. |
| 259 | TS SHALL initialize IB evolution and inquiry‑state metrics to explicit deterministic empty or zeroed defaults at creation. | IB metrics must start from deterministic zeroed defaults. |
| 260 | TS SHALL expose IB metadata to GB through deterministic TP/MP supervisory interface views without exposing IB internal implementation state directly. | GB sees IB metadata but not internal implementation details. |

</details>
<details>
<summary>HLR‑20.030‑261 → 280</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 261 | TS SHALL update IB ΔH%, branching, conflict, usage, and TCU metrics deterministically for each active cycle. | IB evolution metrics must be updated deterministically every cycle. |
| 262 | TS SHALL update IB inquiry‑state metadata deterministically based on OB<->IB interactions. | Inquiry‑state metadata must reflect OB–IB interactions deterministically. |
| 263 | TS SHALL log IB metadata changes with when, why, and how context. | All IB metadata changes must be logged with full context. |
| 264 | GB SHALL consume TP/MP‑exposed IB metadata to supervise IB evolution and MAY issue bounded supervisory actions including Crop, Prune, Dampen, Slow, Suspend, Reshape, and Kill. | GB supervises IB evolution using metadata and can apply bounded corrective actions. |
| 265 | GB MAY request IB promotion or retirement based on deterministic metadata evaluation. | GB can promote or retire IBs based on deterministic criteria. |
| 266 | GB SHALL detect convergent IB sets using metadata and MAY issue an IB‑merge supervisory command. | GB can merge IBs when they converge. |
| 267 | Upon approved IB‑merge, TS SHALL instantiate a merged IB, combine source metadata deterministically, retire source IBs, update RB routing, and log the merge event. | IB merges must be deterministic and fully logged. |
| 268 | GB SHALL detect divergent IB behavior using metadata and MAY issue an IB‑split supervisory command. | GB can split IBs when they diverge. |
| 269 | Upon approved IB‑split, TS SHALL instantiate IB‑X1..IB‑Xn, partition inquiry‑state/evolution metadata deterministically, retire or dampen parent IB state, update RB routing, and log the split event. | IB splits must be deterministic and fully logged. |
| 270 | TS SHALL maintain a bounded pending inquiry buffer for OuB‑tagged incomplete‑meaning snapshots under explicit retention and eviction policy. | Pending inquiry buffer must be bounded and policy‑driven. |
| 271 | TS SHALL NOT instantiate IBs without GB approval and SHALL deterministically discard denied inquiry buffer entries. | IB creation requires GB approval; denied entries must be removed deterministically. |
| 272 | OuB SHALL NOT route directly to IB, and inquiry exchange SHALL remain constrained to OB<->IB paths. | OuB cannot communicate with IB; inquiry flows only through OB. |
| 273 | TS SHALL apply GB supervisory actions, including merge, split, promotion, decomposition, and retirement controls, only at deterministic safe boundaries. | All supervisory actions must occur at deterministic safe boundaries. |
| 274 | TS and GB SHALL log split, merge, IB creation, IB evolution, IB promotion, OB decomposition, and supervisory actions in append‑only, time‑indexed records. | All supervisory lifecycle events must be logged append‑only and time‑indexed. |
| 275 | Supervisory lifecycle logs SHALL remain accessible for deterministic audit and version‑control workflows. | Logs must be accessible for audit and version control. |
| 276 | IB population supervision SHALL enforce global boundedness and stability envelopes while preventing deterministic‑equivalent duplication and runaway inquiry branching. | IB population must remain bounded and stable; no runaway branching. |
| 277 | TS SHALL bind deterministic runtime profiles to execution signatures, including numeric, integer, locale/unicode, parser strictness, PRNG, export, diagnostics, and redaction profiles. | Runtime profiles must be deterministic and tied to execution signatures. |
| 278 | TS SHALL enforce profile‑precedence behavior where execution‑signature profiles override environment defaults. | Execution‑signature profiles override environment defaults deterministically. |
| 279 | TS SHALL enforce strict parser profiles for canonical formats with deterministic malformed‑input reject codes. | Parsing must be deterministic with fixed reject codes. |
| 280 | TS SHALL apply deterministic lexical normalization rules for whitespace, Unicode escapes, and literal‑versus‑escaped character handling. | Lexical normalization must be deterministic. |

</details>
<details>
<summary>HLR‑20.030‑281 → 300</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 281 | TS SHALL enforce deterministic replay‑evidence export behavior, including mode‑specific structures and signature‑bound redaction profiles. | Replay evidence must export deterministically with signature‑bound structure/redaction. |
| 282 | TS SHALL enforce canonical export container/compression behavior including fixed format, fixed compression policy, deterministic chunking/order, and canonical manifest serialization. | Export containers must follow fixed deterministic format, compression, ordering, manifest rules. |
| 283 | TS SHALL apply deterministic empty‑artifact semantics for zero‑event windows. | Zero‑event windows must produce deterministic empty artifacts. |
| 284 | TS SHALL enforce deterministic diagnostics flush behavior and deterministic diagnostics retention/purge behavior. | Diagnostics flush/retention/purge must be deterministic. |
| 285 | TS SHALL isolate concurrent non‑ordering side effects from decision state and signature‑visible artifacts. | Side effects must not influence decision state or signature‑visible outputs. |
| 286 | TS SHALL encode deterministic signature hashing with canonical ordering/serialization and domain‑separated hash preimages. | Signature hashing must be deterministic, canonical, and domain‑separated. |
| 287 | TS SHALL enforce atomic policy activation and deterministic activation‑failure rollback to prior signature state. | Policy activation must be atomic; failures must roll back deterministically. |
| 288 | TS SHALL enforce deterministic unsupported‑feature rejection with fixed audit reason codes. | Unsupported features must be rejected deterministically with fixed codes. |
| 289 | TS SHALL enforce deterministic verifier‑conformance behavior with versioned compatibility windows and deterministic out‑of‑window rejection. | Verifier conformance must be deterministic and version‑bounded. |
| 290 | TS SHALL enforce deterministic test‑oracle and profile‑version drift detection before release acceptance. | TS must detect drift in test oracles/profiles before accepting a release. |
| 291 | TS SHALL define CIL as the functional conversation‑integration layer responsible for deterministic FIFO‑preserving intake, snapshot coherence, deterministic classification, and deterministic supervisory escalation flow. | Defines CIL’s deterministic responsibilities (intake, coherence, classification, escalation). |
| 292 | TS SHALL define CIL control boundaries such that inquiry instantiation requires GB approval, timeout/default handling is policy‑bound, and late‑approval re‑entry behavior is deterministic. | CIL must enforce deterministic supervisory boundaries and approval rules. |
| 293 | TS SHALL define COB as the functional conversation‑object layer responsible for deterministic conversation‑state identity, lineage, promotion/deprecation lifecycle semantics, and deterministic compaction/replay behavior. | Defines COB’s deterministic responsibilities (identity, lineage, lifecycle, replay). |
| 294 | TS SHALL require COB state to be derived from approved canonical semantics, with deterministic redaction/export behavior and deterministic zero‑event artifact semantics. | COB state must be canonical and deterministic in export/redaction/zero‑event behavior. |
| 295 | TS SHALL define COP as an optional functional asynchronous proposal subsystem that is strictly propose‑only and SHALL NOT mutate authoritative decision state outside approved GB/TS boundaries. | COP is propose‑only; cannot mutate authoritative state. |
| 296 | TS SHALL require COP proposal handling to preserve deterministic provenance, bounded queueing/fairness, deterministic overload policy, and deterministic safe‑boundary visibility rules. | COP proposals must be deterministic, provenance‑tracked, bounded, and fair. |
| 297 | TS SHALL enforce execution‑signature‑bound policy/profile precedence and deterministic unsupported‑state reject behavior consistently across CIL, COB, and COP flows. | Execution signatures govern deterministic behavior across all conversation layers. |
| 298 | End‑to‑end conversation execution SHALL preserve deterministic flow across CIL intake/routing, TS/GB supervision, COB lifecycle updates, and optional COP proposals. | End‑to‑end conversation execution must be deterministic across all layers. |
| 299 | 20.32 COB, 20.33 CIL, and 20.34 COP SHALL serve as normative functional decomposition modules and SHALL satisfy all invariants defined in 20.30 and 20.10. | COB, CIL, and COP must satisfy all invariants and serve as normative modules. |
| 300 | Functional interfaces among CIL, COB, COP, TS core, and GB SHALL be explicit, versioned, testable, auditable, and traceable to module‑level requirements. | All interfaces must be explicit, versioned, testable, auditable, and traceable. |

</details>
<details>
<summary>HLR‑20.030‑301 → 320</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 301 | COP proposals that affect conversation integration or conversation‑object state SHALL become effective only via GB‑approved supervisory actions at deterministic safe boundaries; COP SHALL NOT directly mutate CIL, COB, or TS‑core decision state. | COP is propose‑only; any effect requires GB approval at safe boundaries. |
| 302 | COP suitability SHALL be limited to bounded, deterministic, provenance‑rich proposal‑processing work expressible as metadata transformation, queue management, or advisory commit preparation. | COP can only perform bounded, deterministic metadata‑level proposal work. |
| 303 | COP SHALL be unsuitable for authoritative meaning construction, direct state mutation, unbounded search, wall‑clock precedence decisions, or tasks requiring nondeterministic internal state. | COP cannot generate meaning or perform nondeterministic or state‑mutating tasks. |
| 304 | RB SHALL apply a deterministic constant‑time policy check to each candidate OBx→OBy relation at a mandatory deterministic safe boundary. | RB must enforce relation safety deterministically in constant time. |
| 305 | Unsafe relations SHALL be blocked and SHALL NOT influence TS state, OB state, RB state, TB state, IB state, CIL state, COB state, or output. | Unsafe relations must be fully blocked with zero downstream influence. |
| 306 | For each blocked relation, RB SHALL record blocked‑relation evidence including OBx class, OBy class, RBxy identifier, policy reason code, and timestamp/tp_seq. | Blocked relations must be logged with full metadata. |
| 307 | Deployments MAY enable GB notification for blocked relations; when enabled, RB SHALL emit exactly one supervisory signal per blocked relation event. | Optional GB notifications must be deterministic and one‑per‑event. |
| 308 | RB safety‑policy enforcement SHALL maintain low CPU overhead and SHALL execute in O(1) time per relation. | Safety checks must be constant‑time and efficient. |
| 309 | RB safety‑policy enforcement SHALL NOT perform relation scanning, semantic inference, or additional TS‑cycle scheduling. | RB cannot perform heavy or semantic operations during checks. |
| 310 | RB relationship safety enforcement SHALL be mandatory at the defined safe boundary and SHALL NOT be bypassed by runtime mode, profile, or subsystem configuration. | Safety enforcement cannot be disabled or bypassed. |
| 311 | TP SHALL include a deterministic boolean field `tr_needs_update` indicating whether TP.TR is stale for the current TP and MTP semantic context. | TP must track whether TR recomputation is required. |
| 312 | Any module that modifies semantic‑relevant TP fields SHALL set `tr_needs_update = true`. | Any semantic change triggers TR recomputation. |
| 313 | RB SHALL route the TP to the TR routine if and only if `tr_needs_update = true`. | TR runs only when the staleness flag is true. |
| 314 | The TR routine SHALL clear `tr_needs_update = false` only after successful TR recompute and commit; on reject/error/failure paths it SHALL remain true. | TR clears the flag only after successful recompute. |
| 315 | TR SHALL NOT execute on cycles where `tr_needs_update = false`. | TR must not run unnecessarily. |
| 316 | DCB SHALL observe the TP trajectory after OB processing and before the TR routine within the same deterministic cycle. | DCB observes geometric trajectory between OB and TR. |
| 317 | DCB SHALL emit directional‑change events consumed by the TR routine as ephemeral geometric inputs. | DCB outputs ephemeral geometric signals for TR. |
| 318 | DCB SHALL NOT write to TP fields, SHALL NOT interpret meaning or semantics, and SHALL NOT be consumed directly by RB. | DCB is strictly non‑semantic and cannot modify TP. |
| 319 | DCB directional‑change events SHALL be consumed only when `tr_needs_update = true` and RB routes the TP to TR. | DCB events are used only when TR is triggered. |
| 320 | DCB directional‑change events SHALL be recomputable from persisted TP trajectory state for deterministic replay. | DCB events must be replay‑deterministic. |

</details>
<details>
<summary>HLR‑20.030‑321 → 340</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 321 | DCB feedback SHALL remain bounded, non‑recursive, and non‑expansive per 20.106 and 20.165. | DCB feedback must be strictly bounded; no recursion or expansion. |
| 322 | Pipeline A (meaning construction) SHALL execute the basin pipeline defined in sections 2–3 and SHALL be the sole writer of `semantic_core` per 20.12. | Pipeline A exclusively writes semantic_core; follows basin pipeline rules. |
| 323 | Pipeline B (Execution Manifold) SHALL execute realization planning and expression via OpBeh, OBG, XlateR, SRP, TrigRB, and IMR per 20.12 invariants. | Pipeline B handles realization/expression using specific modules. |
| 324 | Cross‑pipeline interaction SHALL be limited to read‑only semantic snapshots into Pipeline B and metadata‑only writes (`exec_plan`, `exec_trace`) from Pipeline B; IMR feedback into Pipeline A SHALL use bounded, auditable trigger pathways only. | Pipeline A/B boundaries: B reads semantics, writes metadata; IMR feedback is bounded. |
| 325 | Pipeline A SHALL include ISc (Inference Scorer) as a deterministic scoring stage… ISc SHALL NOT generate meaning, expand candidates, or perform semantic interpretation. | ISc scores but cannot generate or interpret meaning. |
| 326 | Escalation from ISc to COP SHALL occur only through a propose‑only, GB‑supervised, deterministic safe boundary. COP SHALL NOT mutate TP and SHALL return only deterministic refinements. | ISc→COP escalation is controlled; COP cannot mutate TP. |
| 327 | COP outputs SHALL be treated as Pipeline A advisory distributions. COP SHALL NOT introduce new semantics, SHALL NOT alter TP structure, and SHALL NOT bypass ΔH% accounting or TB interpretation. | COP outputs are advisory only; cannot change semantics or TP structure. |
| 328 | Merge SHALL produce immutable `tp_update_request` envelopes for TPU. These envelopes SHALL be structurally validated only and SHALL NOT be semantically interpreted or modified by TPU. | Merge→TPU contract: TPU validates structure only; no semantic interpretation. |
| 329 | TPU SHALL be the sole writer to TP. All Pipeline A commit operations SHALL occur only through TPU. | TPU exclusively writes TP; all commits flow through TPU. |
| 330 | The combined ISc → COP → Merge → TPU functional path SHALL preserve full replay equivalence… Pipeline A SHALL yield identical TP(N+1). | Entire scoring→proposal→merge→commit path must be replay‑deterministic. |
| 331 | TS core execution SHALL be deterministic for identical (input, MTP state, seed) tuples. | Core determinism invariant. |
| 332 | Meaning representation SHALL remain explicit, structured, and stored in the MTP, not in high‑dimensional vectors. | Meaning must be explicit and MTP‑based; no latent vectors. |
| 333 | OB, RB, TB, and TS‑core modules SHALL remain separable, inspectable, and testable. | Architecture must be modular and inspectable. |
| 334 | All TS reasoning steps… SHALL remain visible and auditable. | Reasoning must be interpretable and auditable. |
| 335 | Meaning construction SHALL combine OB/RB path semantics, ΔH% contributions, ΔH% missing mass, and TB interpretations deterministically. | Defines deterministic meaning‑construction formula. |
| 336 | Variability SHALL be introduced only in designated variability modules and SHALL NOT affect TS core determinism. | Variability is isolated from core cognition. |
| 337 | The TS core SHALL remain fully deterministic and SHALL NOT depend on random seeds. | Random seeds cannot influence meaning. |
| 338 | The response‑generator seed SHALL be pseudo‑random, documented, isolated, and SHALL NOT influence meaning construction. | Seed affects expression only, not meaning. |
| 339 | TS SHALL achieve deterministic replay for identical (input, MTP state, seed) tuples across all basins and meaning‑construction stages. | Global replay determinism across all basins. |
| 340 | RB SHALL route the TP to the TR routine if and only if `tr_needs_update = true`. | TR runs only when the staleness flag is set. |

</details>
<details>
<summary>HLR‑20.030‑341 → 351</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 341 | Any OB that modifies semantic‑relevant TP fields SHALL set `tr_needs_update = true`. | OB must mark TR as stale whenever it changes semantic TP fields. |
| 342 | DCB SHALL emit deterministic directional‑change events based on bounded geometric invariants. | DCB events must be deterministic and based on fixed geometric rules. |
| 343 | DCB SHALL NOT write TP fields and SHALL NOT interpret meaning. | DCB is strictly non‑semantic and cannot modify TP. |
| 344 | TR SHALL consume DCB events only when `tr_needs_update = true` and RB routes to TR. | DCB events are used only when TR is legitimately triggered. |
| 345 | Any module that modifies semantic‑relevant TP fields during merge SHALL set `tr_needs_update = true`. | Merge operations that change semantics must trigger TR recomputation. |
| 346 | TS SHALL count only deterministic, state‑changing transitions as MCS and SHALL prohibit micro‑step inflation. | Only meaningful state‑changing transitions count as MCS; no inflation. |
| 347 | Each MCS SHALL consume a deterministic TCU envelope derived from subsystem budgets, invariant under identical execution signatures. | Each MCS has a deterministic TCU cost. |
| 348 | Truth and completion evaluation SHALL be deterministic and SHALL use structured truth_hypotheses and H% semantics. | Truth/done evaluation must be deterministic and structured. |
| 349 | Splitting SHALL be deterministic, rule‑based, explicit, and loss‑accounted through ΔH% semantics. | Splitting must follow deterministic rules and track ΔH% loss. |
| 350 | Merging SHALL recombine lane TPs into the MTP deterministically using ΔH%, TB interpretations, and prior MTP state. | Merging must be deterministic using ΔH% + TB + prior state. |
| 351 | The response‑generator seed SHALL NOT influence meaning construction and SHALL affect only expressive variability. | Seed affects expression only; meaning remains deterministic. |

</details>
