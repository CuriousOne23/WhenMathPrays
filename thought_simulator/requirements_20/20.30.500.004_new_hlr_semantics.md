# 20.30.500.010 — TS Operational Semantics (Semantic Table)

<details>
<summary>HLR‑20.30.010‑001 → 007</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | Pipeline A SHALL execute the TS cycle in the canonical deterministic order defined in §7. | Cycle ordering is fixed and deterministic; Pipeline A must follow the exact sequence. |
| 002 | Lane creation, execution, and merge SHALL be deterministic and SHALL NOT introduce nondeterministic expansion. | Lane semantics must be deterministic; no nondeterministic branching or expansion. |
| 003 | TPs SHALL be ephemeral lane‑local objects; MTP SHALL be the sole global semantic state. | TP is per‑lane/per‑cycle only; MTP is the only persistent semantic state. |
| 004 | Pipeline A SHALL guarantee replay equivalence for identical (input, MTP state) tuples. | Replay determinism: identical input + MTP → identical meaning. |
| 005 | `mtp_update` SHALL freeze semantic_core and SHALL be the sole handoff point to Pipeline B. | A/B boundary is fixed; only `mtp_update` commits meaning to B. |
| 006 | Pipeline A SHALL write meaning; Pipeline B SHALL express meaning without modifying it. | Strict separation: A constructs meaning; B expresses it but cannot change it. |
| 007 | Pipeline A SHALL detect, tag, and degrade deterministically under overflow conditions. | Overflow handling must be deterministic with tagging + bounded degradation. |

</details>
# 20.30.500.020 — TS Cycle Allocation & Stability Envelope (Semantic Table)

<details>
<summary>HLR‑20.30.020‑001 → 015</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | The TS SHALL enforce a deterministic global maximum for active lanes per cycle. | Lane count per cycle must be capped and enforced deterministically. |
| 002 | The TS SHALL enforce a deterministic maximum number of OB activations per cycle. | OB activation count must be bounded to prevent runaway branching. |
| 003 | The TS SHALL enforce a deterministic maximum routing fan‑out per TP to prevent exponential lane growth. | Routing fan‑out must be capped to avoid exponential lane explosion. |
| 004 | The TS SHALL enforce deterministic maximum evidence payload sizes for OB outputs. | OB evidence size must be bounded. |
| 005 | The TS SHALL enforce deterministic maximum TB interpretation channel sizes and hypothesis counts. | TB outputs must be bounded in size and number. |
| 006 | The TS SHALL enforce a deterministic maximum provenance chain depth. | Provenance depth must be capped. |
| 007 | The TS SHALL enforce deterministic maximum ΔH% contribution per basin per cycle. | ΔH% contributions must be bounded. |
| 008 | The TS SHALL enforce deterministic per‑cycle compute envelopes for all basins. | Each basin has a deterministic compute budget per cycle. |
| 009 | The TS SHALL detect overflow deterministically within the same cycle in which it occurs. | Overflow detection must be immediate and deterministic. |
| 010 | The TS SHALL tag overflow events with deterministic stability metadata accessible to supervisory and user‑facing layers. | Overflow must be explicitly tagged and visible. |
| 011 | Overflow SHALL result in deterministic, policy‑bounded degradation behavior. | Overflow triggers deterministic degradation actions. |
| 012 | The TS SHALL NOT silently drop lanes, evidence, or interpretation fields without deterministic tagging and logging. | No silent data loss allowed. |
| 013 | Overflow SHALL NOT trigger probabilistic reconstruction or latent‑state inference. | Overflow recovery must be deterministic; no hallucination. |
| 014 | The TS SHALL expose deterministic stability metadata in the MTP snapshot. | Stability metadata must be included in MTP snapshots. |
| 015 | Overflow and degradation metadata SHALL conform to the canonical schema defined in this file. | All overflow metadata must follow the canonical schema. |

</details>
# 20.30.500.030 — TCU Budgeting & Deterministic Degradation (Semantic Table)

<details>
<summary>HLR‑20.30.030‑001 → 009</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | The TS SHALL define a normalized Thought Compute Unit (TCU) using a deterministic calibration kernel. | TS must define TCUs via a fixed calibration kernel to normalize compute cost. |
| 002 | The TS SHALL define deterministic min/typ/max TCU budgets for each subsystem in the TS cycle. | Each subsystem must have deterministic TCU budgets (min/typ/max). |
| 003 | Each subsystem SHALL operate within its defined TCU envelope unless deterministic degradation is applied. | Subsystems must stay within budget unless deterministic degradation triggers. |
| 004 | When a subsystem exceeds its TCU budget, the TS SHALL apply deterministic, policy‑bounded degradation behavior. | Exceeding budget triggers deterministic, policy‑bounded degradation. |
| 005 | Degradation SHALL NOT involve probabilistic reconstruction, latent‑state inference, or nondeterministic fallback behavior. | Degradation must be deterministic; no probabilistic or latent inference allowed. |
| 006 | The TS SHALL produce a deterministic per‑cycle compute report including subsystem TCU usage and any applied degradation. | TS must emit a deterministic compute report each cycle. |
| 007 | Degradation outcomes SHALL be identical for identical (input, MTP state, execution signature) tuples. | Degradation must be replay‑safe and deterministic. |
| 008 | The TS SHALL attribute ΔH%‑related compute cost deterministically in the per‑cycle compute report. | ΔH% compute cost attribution must be deterministic. |
| 009 | The TS SHALL include deterministic MTP update cost in the per‑cycle compute report. | MTP update cost must be included deterministically in the compute report. |

</details>
# 20.30.500.040 — Meaningful Cognitive Step (MCS) Rules (Semantic Table)

<details>
<summary>HLR‑20.30.040‑001 → 008</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | An MCS SHALL represent a deterministic, state‑changing transition that advances the TS toward a declared success condition. | MCS = meaningful, deterministic semantic progress toward a goal. |
| 002 | An MCS SHALL correspond to a validated change in TP or MTP semantic state. | MCS requires an actual semantic state change (TP or MTP). |
| 003 | Each MCS SHALL be attributable to a specific subsystem and a specific TP/MTP transition. | Every MCS must identify which subsystem caused which state change. |
| 004 | The TS SHALL prohibit subdivision of a single semantic action into multiple logged steps. | No micro‑step inflation; one semantic action = one MCS. |
| 005 | The TS SHALL prohibit repeated or redundant artifact emissions from being counted as MCS events. | Duplicate or redundant outputs cannot inflate MCS count. |
| 006 | The TS SHALL prohibit no‑ops, formatting changes, and deterministic replays of identical state from being counted as MCS events. | No‑ops, formatting, or unchanged replays do not count as MCS. |
| 007 | The TS SHALL log each MCS deterministically with before/after state, subsystem attribution, and cycle index. | MCS logging must be deterministic and fully attributable. |
| 008 | MCS counts SHALL be identical for identical (input, MTP state, execution signature) tuples. | MCS behavior must be replay‑safe and deterministic. |

</details>
# 20.30.500.050 — Merge Formal Semantics (Semantic Table)

<details>
<summary>HLR‑20.30.050‑001 → 008</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | Merge SHALL recombine lane‑local TPs into a single merged TP using deterministic, field‑wise rules. | Merge must be deterministic and field‑specific; all lanes recombine into one TP. |
| 002 | Merge SHALL NOT invent new semantics and SHALL only recombine existing lane‑local and prior‑MTP fields. | Merge cannot create new meaning; it only recombines what already exists. |
| 003 | Merge SHALL process lane TPs in a deterministic, non‑configurable order. | Lane order is fixed and deterministic; no runtime variability. |
| 004 | Merge SHALL apply field‑specific deterministic merge rules for all semantic fields. | Each field has a defined merge rule (max, sum, overwrite, union, etc.). |
| 005 | Merge SHALL integrate ΔH% contributions and missing‑mass adjustments deterministically. | ΔH% must be combined deterministically with proper missing‑mass handling. |
| 006 | Merge SHALL integrate TB interpretation outputs deterministically without reinterpretation. | TB outputs are recombined structurally; Merge does not reinterpret them. |
| 007 | Merge SHALL produce a deterministic TPU update envelope containing the merged TP and associated metadata. | Merge must emit a deterministic envelope for TPU commit. |
| 008 | Merge results SHALL be identical for identical sets of lane TPs, prior MTP state, and merge rules. | Merge must be fully replay‑equivalent across hardware and runs. |

</details>
# 20.30.500.060 — Truth & Completion Semantics (Semantic Table)

<details>
<summary>HLR‑20.30.060‑001 → 008</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | Truth evaluation SHALL interpret TB outputs, ΔH% contributions, and prior MTP truth state using deterministic rules. | Truth evaluation must be deterministic and based only on structured inputs. |
| 002 | Truth hypotheses SHALL be represented as structured objects containing proposition, evidence, uncertainty, and truth state. | Truth hypotheses must use a structured, explicit schema. |
| 003 | H% SHALL be computed deterministically from ΔH%, missing‑mass indicators, TB outputs, and prior uncertainty state. | Uncertainty (H%) must be deterministic and structure‑based. |
| 004 | Truth evaluation SHALL NOT use probabilistic inference, latent‑space estimation, or stochastic sampling. | No probabilistic or latent inference allowed in truth evaluation. |
| 005 | Completion evaluation SHALL determine done/not‑done states using deterministic rules based on truth stability and uncertainty. | Completion must be deterministic and based on truth + H% stability. |
| 006 | Truth evaluation SHALL precede completion evaluation within the same cycle. | Truth evaluation always happens before completion. |
| 007 | Truth and completion outcomes SHALL be identical for identical (input, MTP state, execution signature) tuples. | Truth/completion must be replay‑safe and deterministic. |
| 008 | Truth and completion results SHALL be included in the deterministic TPU update envelope. | Truth/completion outputs must be passed to TPU for commit. |

</details>

# 20.30.500.070 — TPU & MTP Update Semantics (Semantic Table)

<details>
<summary>HLR‑20.30.070‑001 → 009</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | TPU SHALL be the sole writer to the MTP and SHALL perform all semantic commits. | TPU exclusively commits meaning; no other module may write to MTP. |
| 002 | TPU SHALL validate the update envelope structurally before committing it. | TPU must perform deterministic structural validation of merge output. |
| 003 | TPU SHALL apply updates in a deterministic, canonical order. | Commit ordering is fixed, deterministic, and non‑configurable. |
| 004 | TPU SHALL update the MTP atomically within a single deterministic commit step. | MTP updates must be atomic and occur as one indivisible operation. |
| 005 | TPU SHALL generate a deterministic `commit_id` for each successful commit. | Every commit produces a deterministic, replay‑safe commit ID. |
| 006 | TPU commits SHALL produce identical MTP(N+1) for identical (input, MTP state, execution signature) tuples. | Commit behavior must be fully replay‑equivalent. |
| 007 | `mtp_update` SHALL freeze the MTP and SHALL be the sole handoff point to Pipeline B. | A/B boundary is immutable; only `mtp_update` hands meaning to B. |
| 008 | Pipeline B SHALL NOT modify the MTP and SHALL treat it as immutable. | Pipeline B is strictly read‑only with respect to meaning. |
| 009 | TPU SHALL expose deterministic commit metadata, including overflow/stability information, to supervisory layers. | TPU must publish commit metadata for GB/MB visibility. |

</details>
# 20.30.500.080 — Response Generation Semantics (Semantic Table)

<details>
<summary>HLR‑20.30.080‑001 → 008</summary>

| HLR # | SHALL Text | Semantic Summary |
|-------|------------|------------------|
| 001 | The RG SHALL treat the MTP as immutable and SHALL NOT modify meaning. | RG cannot change meaning; MTP is read‑only. |
| 002 | The RG SHALL produce deterministic realization behavior for a fixed (MTP, seed) tuple. | Realization is deterministic when MTP and seed are fixed. |
| 003 | Expressive variability SHALL be controlled exclusively by the response‑generator seed. | Only the seed may introduce stylistic variation. |
| 004 | The RG SHALL NOT perform semantic inference, reinterpret meaning, or alter truth/completion state. | RG is expression‑only; no semantic reasoning allowed. |
| 005 | For identical (MTP, seed) tuples, the RG SHALL produce identical surface‑form output. | Replay‑safe expression: identical inputs → identical output. |
| 006 | The RG SHALL map semantic fields to surface‑form constructs using deterministic rules. | Mapping from meaning → language is deterministic. |
| 007 | The RG SHALL operate strictly after `mtp_update` and SHALL NOT access pre‑commit semantic state. | RG must stay on the B‑side of the A/B boundary. |
| 008 | The RG SHALL NOT write to TP, MTP, or any Pipeline A structure. | RG has no write‑back privileges; expression only. |

</details>

