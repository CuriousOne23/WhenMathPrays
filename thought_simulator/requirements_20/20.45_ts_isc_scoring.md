# **20.45 — TS Inference Scorer (ISc) — Rewritten (GitHub‑Math Compatible)**  
**Document ID:** 20.45  
**Version:** 2.0 (Structural Optimization + Math Revision)  
**Status:** Normative  
**Owner:** TS Architecture  
**Scope:** Deterministic scoring primitive in Path‑A  

---

# **1. Purpose** *(Informative)*

The TS Inference Scorer (ISc) is a **deterministic scoring primitive** in Path‑A.  
Its role is to evaluate a **finite candidate set** of interpretations or state transitions derived from:

- **ContextEnvelope (CE)**  
- **IntakeEnvelope (IE)**  
- **Upstream inference wiring (CEx)**  

ISc:

ISc:

- does not generate new semantic propositions or expand the candidate set
- performs meaning evaluation, discrimination, weighting, uncertainty analysis, and stabilization
- identifies invariant meaning (the semantic mode) using both semantic-layer and structural-layer features
- produces a normalized scoring distribution over candidate interpretations
- computes entropy, ΔH%, confidence, and rationale codes
- may trigger COP escalation when policy thresholds are met  

ISc strengthens ambiguity detection, uncertainty propagation, and meaning stability while preserving TS invariants and safe‑boundary rules.

---

# **2. Scope** *(Informative)*

ISc operates **only** in the inference path:

```
InB → IIInB → IE → CEx → CE → TPU → SOB → SROB → CnOB → SmOB → WrdNm → ISc → SSG
```

ISc is **not**:

- a basin  
- a coprocessor  
- a realization component  
- a TP subsystem  

ISc does **not**:

- read CIL directly  
- invoke IIInB  
- interact with IB, TB, GBIB, or GB  
- write directly to TP  

---

# **3. Architectural Position** *(Informative)*

ISc appears only in the **Inference Path**, not in:

- Normal Path  
- Clarification Path  
- Post‑TS Path  

Merge is internal to TPU.

---

# **4. Definitions** *(Informative)*

- **Candidate Set** — finite interpretations or transitions derived from CE + IE.  
- **ISc Distribution** — normalized probability distribution over candidates.  
- **Entropy / ΔH%** — structural‑uncertainty metrics.  
- **Rationale Codes** — interpretable feature‑level explanations.

---

# **5. Core Scoring Requirements (Normative)**

### **HLR‑20.45‑002 — CE‑Derived Candidate Set**  
ISc SHALL operate exclusively on a finite candidate set derived from CE and IE.

### **HLR‑20.45‑003 — No Meaning Generation**  
ISc SHALL NOT generate new meanings or expand the candidate set, but performs meaning evaluation, discrimination, weighting, uncertainty analysis, and stabilization.  

### **HLR‑20.45‑004 — Deterministic Scoring**  
ISc SHALL produce identical outputs for identical inputs, state, and seed.

### **HLR‑20.45‑005 — Transparent Features**  
ISc SHALL use interpretable scoring features and SHALL log all features in rationale metadata.

### **HLR‑20.45‑006 — Normalized Distribution**  
ISc SHALL output a normalized probability distribution over the candidate set.

### **HLR‑20.45‑007 — Uncertainty Metrics**  
ISc SHALL compute entropy, ΔH%, confidence, and rationale codes using the unified ΔH% definition.

### **HLR‑20.45‑008 — COP Escalation**  
ISc SHALL trigger COP escalation when policy thresholds are met.

### **HLR‑20.45‑009 — No TP Mutation**  
ISc SHALL NOT write directly to TP; outputs SHALL reach TP only via Merge → TPU.

### **HLR‑20.45‑010 — Replay Safety**  
ISc SHALL satisfy replay invariants.

### **HLR‑20.45‑011 — TCU Bound**  
ISc SHALL operate within the TCU budget defined in 20.90.

### **HLR‑20.45‑012 — Inference Path Only**  
ISc SHALL operate only in the inference path and SHALL NOT interact with Pipeline B.

---

# **6. FFTM‑Based Semantic Scoring (Four Field Token Model) (Normative)**

### **HLR‑20.45‑058 — FFTM Feature Consumption**  
ISc SHALL score candidates using the four FFTM meaning fields:

- token_surface  
- token_base  
- token_expression  
- token_intent  

### **HLR‑20.45‑061 — Meaning‑Layer Weighting**  
ISc SHALL weight meaning‑layer cues (token_expression, token_intent) more heavily than surface‑form cues unless architecture‑level evidence dictates otherwise.

### **HLR‑20.45‑062 — Architecture‑Aligned Weight Tuning**  
ISc MAY adjust FFTM weights only when justified by architecture‑level evaluation.

### **HLR‑20.45‑063 — Replay‑Safe Weight Application**  
ISc SHALL apply FFTM weights using the canonical scoring equation:

$$
\text{score}(c) = w_s \cdot f_s(c) + w_b \cdot f_b(c) + w_e \cdot f_e(c) + w_i \cdot f_i(c)
$$

### **HLR‑20.45‑064 — No Overfitting to Path‑A Tests**  
ISc SHALL NOT tune weights to pass specific Path‑A test items.

### **HLR‑20.45‑065 — Semantic‑Core Integrity**  
ISc SHALL NOT modify semantic_core or FFTM fields.

---

# **6.1 Unified Scoring Feature Set (Informative)**  
ISc evaluates invariant meaning by fusing semantic-layer features, structural-layer features, discourse cues, metadata cues, and next‑turn context cues.  
This section enumerates the complete feature space consumed by ISc.

### **A. Semantic-Layer Features (FFTM / semantic_core)**  
These features define the semantic identity (“flower type”) of each candidate interpretation:

- **token_surface** — lexical surface form  
- **token_base** — lemma/base meaning  
- **token_expression** — expression/mode marker  
- **token_intent** — intent-layer meaning  

ISc applies canonical FFTM weights to these features and treats them as the primary semantic discriminators.

---

### **B. Structural-Layer Features (WrdNm numeric encodings)**  
These features provide bounded, deterministic structural meaning cues:

- **surface_id**  
- **lemma_id**  
- **expression_id**  
- **temporal_id**  
- **causal_id**  
- **continuity_id**  
- **entity_id**  
- **modality**  
- **affect**  
- **underspec**  
- **adjacency**  
- **ordering_id**  
- **constraint_family_id**  
- **constraint_importance**  
- **missing_slot**  
- **routing_id**  
- **transform_id**  
- **identity_id**  
- **next_context_id**  
- **thread_hash**

These features do not generate meaning; they provide **semantic-adjacent structure** that stabilizes interpretation and reduces ambiguity.

---

### **C. Discourse-Context Cues (STPX)**  
Normalized discourse cues contribute structural information about:

- adjacency  
- turn-taking  
- discourse markers  
- structural continuity  
- discourse-level emphasis  

ISc treats these cues strictly as structural features.

---

### **D. TP-Stream Metadata (deterministic, bounded)**  
Applicable metadata includes:

- **context_metadata (CE)**  
- **residue_metadata (OB‑Set)**  
- **semantic_layer_metadata (SSG, STPX)**  
- **identity_metadata (IdOB)**  
- **continuity_metadata (COB, CIL, CST)**  
- **expressive_metadata (IIInB, IE)**  
- **normalization_metadata (IE)**  
- **next_context_metadata (MCB)**  

ISc consumes only metadata relevant to scoring and treats all metadata as read‑only.

---

### **E. Next-Turn Context Fields (CE.context_fields)**  
These fields provide pre-semantic continuity signals across turns:

- topic  
- stance  
- intent  
- continuity  
- direction  
- coherence  
- importance  

ISc includes these fields exactly as provided and does not perform semantic inference on them.

---

### **Unified Meaning Boundary**  
ISc identifies **invariant meaning** (semantic mode) by combining:

- semantic-layer meaning (FFTM)  
- structural-layer meaning cues (WrdNm)  
- discourse cues  
- metadata cues  
- next-turn continuity cues  

ISc does **not** construct comprehensive meaning; it evaluates and stabilizes the meaning produced upstream.

---

# **7. Input Requirements (Normative)**

### **HLR‑20.45‑013 — Immutable Inputs**  
All ISc inputs SHALL be immutable during scoring.

### **HLR‑20.45‑014 — Canonical Candidate Ordering**  
Candidate ordering SHALL follow canonical ordering rules.

### **HLR‑20.45‑015 — Valid Feature Set**  
Features SHALL be interpretable, deterministic, and bounded.

### **HLR‑20.45‑016 — CE‑Bound Structural Context Only**  
ISc SHALL consume structural context only via CE; semantic features SHALL come from semantic_core / FFTM.

### **HLR‑20.45‑059 — Semantic‑Core Feature Source**  
ISc SHALL include meaning‑layer features derived from semantic_core.

---

# **8. Discourse‑Context Cue Handling (Normative)**

### **HLR‑20.45‑066 — Structural Cue Consumption**  
ISc SHALL consume normalized discourse‑context cues emitted by STPX.

### **HLR‑20.45‑067 — Structural‑Only Interpretation**  
ISc SHALL treat discourse cues strictly as structural features.

### **HLR‑20.45‑068 — Deterministic Cue Scoring**  
ISc SHALL apply deterministic scoring to discourse cues.

### **HLR‑20.45‑069 — Rationale Metadata Integration**  
ISc SHALL include discourse cues in rationale metadata when they affect ranking.

### **HLR‑20.45‑083 — TP‑Stream Metadata Consumption**
ISc SHALL consume applicable deterministic TP‑stream metadata when such metadata improves scoring accuracy or stability.

### **HLR‑20.45‑084 — Read‑Only Discipline**
ISc SHALL treat all TP‑stream metadata as read‑only and SHALL NOT modify any TP fields.

### **HLR‑20.45‑085 — Metadata Applicability Rule**
ISc SHALL consume TP‑stream metadata only when such metadata is relevant to scoring and SHALL ignore metadata that is not applicable.

### **HLR‑20.45‑086 — Forbidden Metadata Consumption**
ISc SHALL NOT consume routing_metadata, ΔH%, truth/done fields, lineage fields, or any Pipeline‑B envelopes.

### **HLR‑20.45‑087 — Structural‑Only Interpretation**
ISc SHALL treat all consumed TP‑stream metadata as structural or semantic‑adjacent features and SHALL NOT perform semantic inference.

### **HLR‑20.45‑088 — Rationale Integration**
ISc SHALL include consumed TP‑stream metadata in rationale metadata when such metadata affects scoring or ranking.

# **8.1 TP‑Stream Metadata Consumption (Informative)**

ISc may consume deterministic, bounded, pre‑semantic or semantic‑adjacent metadata present in the TP‑stream when such metadata is applicable to scoring. Applicable metadata includes:

- context_metadata (CE)
- residue_metadata (OB‑Set)
- semantic_layer_metadata (SSG, STPX)
- scoring_metadata (ISc)
- identity_metadata (IdOB)
- continuity_metadata (COB, CIL, CST)
- expressive_metadata (IIInB, IE)
- normalization_metadata (IE)
- next_context_metadata (MCB)

ISc treats all TP‑stream metadata as read‑only.  
ISc ignores metadata that is not applicable to scoring.  
ISc does not consume routing_metadata, ΔH%, truth/done fields, lineage fields, or any Pipeline‑B envelopes.  
ISc interprets TP‑stream metadata only as structural or semantic‑adjacent features.  
ISc may include consumed metadata in rationale metadata when it affects scoring.

---

# **9. Next‑Turn Context Handling (Normative)**

### **HLR‑20.45‑070**  
ISc SHALL read next‑turn context fields exclusively from CE.context_fields{}.

### **HLR‑20.45‑071**  
ISc SHALL treat next‑turn context fields as pre‑semantic structural metadata.

### **HLR‑20.45‑072**  
ISc SHALL include next‑turn context fields exactly as provided.

### **HLR‑20.45‑073**  
ISc SHALL treat next‑turn context fields as structural scoring features.

### **HLR‑20.45‑074**  
ISc SHALL NOT derive next‑turn context fields from clarifying fields or identity‑layer metadata.

### **HLR‑20.45‑075**  
ISc SHALL guarantee deterministic replay of next‑turn context handling.

### **HLR‑20.45‑076**  
ISc SHALL NOT perform semantic inference on next‑turn context fields.

### **HLR‑20.45‑077**  
ISc SHALL preserve next‑turn context continuity across turns.

### **HLR‑20.45‑078**  
ISc SHALL NOT compute coherence or shift; it SHALL only reflect CE values.

### **HLR‑20.45‑079**  
ISc SHALL include next‑turn context fields in TPU update requests exactly as defined.

### **HLR‑20.45‑080**  
ISc SHALL treat missing next‑turn context fields as deterministic empty‑context cases.

### **HLR‑20.45‑081**  
ISc SHALL NOT interact with truth/done fields, routing fields, ΔH%, lineage, or Path‑B semantics when processing next‑turn context.

### **HLR‑20.45‑082**  
ISc SHALL record next‑turn context drops or truncations in rationale metadata.

---

# **10. Output Requirements (Normative)**

### **HLR‑20.45‑017 — Structured Output Object**  
ISc SHALL output the full `isc_output{}` structure.

### **HLR‑20.45‑018 — Canonical Serialization**  
ISc outputs SHALL follow canonical serialization rules.

### **HLR‑20.45‑019 — 1‑TP‑Cycle Lag**  
ISc outputs SHALL become visible only in TP(N+1) after TPU commit.

---

# **11. Merge / TP Interaction (Normative)**

### **HLR‑20.45‑020 — Merge‑Only Commit Path**  
ISc SHALL commit only via Merge → TPU → TP.

### **HLR‑20.45‑021 — Writer Authority Compliance**  
ISc SHALL write only to the isc{} block.

### **HLR‑20.45‑022 — No Direct TP Writes**  
ISc SHALL NOT write directly to TPU or TP.

---

# **12. COP (Contextual Overload Protection) Escalation Requirements (Normative)**

### **HLR‑20.45‑023 — Deterministic Preconditions**  
### **HLR‑20.45‑024 — Proposal Schema Compliance**  
### **HLR‑20.45‑025 — No Direct COP Access**  
### **HLR‑20.45‑026 — Deterministic Returned Distributions**  
### **HLR‑20.45‑027 — Merge Validation**  
### **HLR‑20.45‑028 — Safe‑Boundary Compliance**  
### **HLR‑20.45‑029 — Replay Requirements**

---

# **13. Forbidden Actions (Normative)**

### **HLR‑20.45‑030 — No Meaning Generation**  
### **HLR‑20.45‑031 — No Candidate Expansion**  
### **HLR‑20.45‑032 — No Pipeline‑B Interaction**  
### **HLR‑20.45‑033 — No Intake Repair**  
### **HLR‑20.45‑034 — No TP Mutation**  
### **HLR‑20.45‑036 — No Bypass of Merge or TPU**  
### **HLR‑20.45‑037 — No Nondeterministic Methods**  
### **HLR‑20.45‑060 — No Semantic‑Core Mutation**

---

# **14. Error Detection Requirements (Normative)**

### **HLR‑20.45‑038 — Candidate‑Set Errors**  
### **HLR‑20.45‑039 — Feature Errors**  
### **HLR‑20.45‑040 — Distribution Errors**  
### **HLR‑20.45‑041 — Policy Errors**  
### **HLR‑20.45‑042 — TCU Errors**  
### **HLR‑20.45‑043 — COP Errors**  
### **HLR‑20.45‑044 — Forbidden‑Action Errors**

---

# **15. Error Handling Requirements (Normative)**

### **HLR‑20.45‑045 — Deterministic Fallback**  
### **HLR‑20.45‑046 — Replay‑Safe Handling**  
### **HLR‑20.45‑047 — Error Object Emission**  
### **HLR‑20.45‑048 — No TS Halt**  
### **HLR‑20.45‑049 — Audit Logging**

---

# **16. Canonical Ordering & Serialization (Normative)**

### **HLR‑20.45‑050 — Canonical Ordering**  
### **HLR‑20.45‑051 — Deterministic Hashing**  
### **HLR‑20.45‑052 — Canonical Audit Serialization**

---

# **17. Verification Requirements (Normative)**

### **HLR‑20.45‑053 — Replay Tests**  
### **HLR‑20.45‑054 — Ordering Tests**  
### **HLR‑20.45‑055 — COP Escalation Tests**  
### **HLR‑20.45‑056 — TCU Degradation Tests**  
### **HLR‑20.45‑057 — Forbidden‑Action Tests**

---

# **End of Document — 20.45 (Rewritten, Option C)**

---
