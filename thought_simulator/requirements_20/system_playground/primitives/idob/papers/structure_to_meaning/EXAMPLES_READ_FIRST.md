# Read before `meaninging_examples_patha_inb_to_ouba.md`

That file is **feel** — worked utterances and nested score bags from an earlier draft.

Live output of IdOB is the **11 packet** (`tp.idob`): key, candidates, \(M\), \(M'\), Δh, flags, residue.  
It is not an OuBA document and not a listener-uptake claim.

Use the examples to walk around contrasts. Use `path_a/identity/idob_testbench.yaml` for utterance + input + output traces that match `idob.py`.
