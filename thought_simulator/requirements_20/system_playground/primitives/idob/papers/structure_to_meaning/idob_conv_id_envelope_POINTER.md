# POINTER — CIE / identity envelope

Live CIE: conversational identity/stance **pressure** on the meaning stand-in, not a second meaning space and not the 10-state geometry walk.

\(M' = M + \alpha I\) with \(I\) from `cie_id`.

Read: `testbenches/idob_structure_to_meaning/05_cie/README.md`  
Examples: `05_cie/cie.examples.yaml`  
Code: `05_cie/modulate.py` called from `11_idob_core/idob.py`

`idob_conv_id_envelope.md` in this folder is historical wording. `hold_geometry` on the packet may still echo a lifecycle name (`formation` default) as an envelope label only.
