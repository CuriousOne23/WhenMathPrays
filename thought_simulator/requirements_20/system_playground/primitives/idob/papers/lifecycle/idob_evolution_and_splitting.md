# LIFECYCLE — not the S2M hop

This document describes split/merge of **identity basins** (formation…closure science).

It is **not** Slide 10 (residue → expand hint) and **not** how `11_idob_core` births \(M\).

RB chooses the next IdOB. IdOB does not split the routing basin.

---

The historical body follows in git if you need the long essay; this file is the warning label plus the original title. Recover the full text from git history of `papers/idob_evolution_and_splitting.md` before it was moved here if the body below was replaced.
