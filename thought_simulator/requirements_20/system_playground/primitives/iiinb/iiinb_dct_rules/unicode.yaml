# IIInB Dictionary Rules — Unicode Noise Normalization
# Canonical family: unicode.normalized

rules:
  "�": ""        # strip replacement character
  "∩┐╜": " "     # replace mojibake with a space

