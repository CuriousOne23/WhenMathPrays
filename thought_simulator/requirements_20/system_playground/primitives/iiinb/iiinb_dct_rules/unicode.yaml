# IIInB Dictionary Rules — Unicode Noise Normalization
# Canonical family: unicode.normalize
# Proposal‑only (IIInB does NOT apply these replacements)

rules:
  "é": "é"        # NFC stabilization
  "é": "é"       # NFD → NFC
  "\uFFFD": "\uFFFD"   # preserve replacement character


