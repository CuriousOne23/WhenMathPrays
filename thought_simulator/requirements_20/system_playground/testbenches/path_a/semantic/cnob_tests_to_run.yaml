# ============================================================
#  CnOB Tests To Run (Version 1.0)
# ============================================================

primitive: CnOB
version: "1.0"

tests:
  - id: cnob_test_001
    description: "Single declarative + operator → C1 exists + operator; no conflict"
    enabled: true

  - id: cnob_test_002
    description: "List with parent/child → C2/C4"
    enabled: true

  - id: cnob_test_003
    description: "Empty text segment → missing_slot"
    enabled: true

  - id: cnob_test_004
    description: "Interrogative, no operator → underspecification"
    enabled: true

  - id: cnob_test_005
    description: "precision + conciseness → conflict Q8"
    enabled: true

  - id: cnob_test_006
    description: "anchor_like importance → constraint_anchor"
    enabled: true
