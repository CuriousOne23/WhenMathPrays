# ============================================================
#  TPU Tests To Run (Version 1.0)
#  Multi-test TPU testbench execution
# ============================================================

primitive: TPU
version: "1.0"

tests:
  - id: tpu_test_001
    description: "Full-field happy-path atomic commit + provenance + audit"
    enabled: true

  - id: tpu_test_002
    description: "Writer-authority violation → deterministic reject + audit"
    enabled: true

  - id: tpu_test_003
    description: "Empty tp_update_request → no-op commit with audit"
    enabled: true
