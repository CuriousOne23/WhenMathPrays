# S2M IdOB tests. Lifecycle formation..closure ids live in idob_lifecycle_archive.yaml.

tests:
  - id: idob_s2m_01_rock
    enabled: true
    description: Rock burst card — birth with leftover residue_code

  - id: idob_s2m_02_deadline
    enabled: true
    description: Deadline card — single candidate, no card residue

  - id: idob_s2m_03_sleepy
    enabled: true
    description: Sleepy card — birth, path_b_eligible

  - id: idob_s2m_04_unmapped
    enabled: true
    description: Unmapped card — empty map, no M

  - id: idob_s2m_05_miss
    enabled: true
    description: Utterance with no pack cues — unassigned, utterance still recorded

  - id: idob_s2m_06_write_boundary
    enabled: true
    description: routing_filter on input must survive process
