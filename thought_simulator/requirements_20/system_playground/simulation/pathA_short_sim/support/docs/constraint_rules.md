# constraint_rules.yaml

## Routing-Relevant Constraint Output
The simulator now extracts:
- matched constraints
- unmatched constraints
- constraint residue

These values are used by routing to select minimal IdOB operators.
