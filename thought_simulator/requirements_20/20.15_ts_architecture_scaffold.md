# **20.40.010 — SOB Requirements (Updated for Path‑A Cognitive‑Field Model)**  
**Structural OB Layer (SOB)**  
**Document ID:** 20.40.010  
**Version:** 1.1 (Cognitive‑Field Update)  
**Status:** Normative  
**Parent:** 20.40_ob_requirements.md  
**Scope:** Requirements for the Structural OB (SOB) layer in Path A  
**Purpose:** Define mandatory behavior, invariants, residue‑handling rules, and bounded semantic‑adjacent interpretation responsibilities for the first OB layer.

---

# **1. Purpose** *(Informative)*

The Structural OB (SOB) layer is the **first classification layer** in Path A.  
Its role is to extract **structural and bounded semantic‑adjacent cues** from the TP, producing:

- a structured TP  
- structural metadata  
- structural‑importance metadata  
- residue fragments  

SOB performs:

- segmentation  
- modality detection  
- operator‑hint extraction  
- domain‑hint extraction  
- tone‑hint extraction  
- constraint‑hint extraction  
- structural‑importance detection (bounded semantic‑adjacent)

SOB performs **bounded meaning extraction** only to the extent required for structural classification and structural‑importance detection.  
SOB does **not** resolve semantic roles, infer intent, or perform meaning construction.  
Its output prepares the TP for deterministic refinement by SROB and deeper meaning construction by downstream OB layers.

---

# **2. Functional Overview** *(Informative)*

SOB:

- segments TP into structural units  
- extracts structural hints and bounded semantic‑adjacent cues  
- produces residue fragments  
- emits structural‑importance candidates (entities and anchors)  
- preserves ordering and structural integrity  
- ensures deterministic, reproducible metadata  
- reduces entropy before SROB  
- prepares residue for routing by RB  

SOB operates in a **bounded semantic‑adjacent cognitive field**, performing structural interpretation supported by upstream metadata without entering semantic‑role or intent inference.

---

# **3. Normative Requirements (HLRs)**  
*(All original requirements preserved; new requirements appended beginning at HLR‑20.40.010‑041.)*

---

## **3.1 Structural Segmentation Requirements**

### **HLR‑20.40.010‑001**  
SOB SHALL segment the input TP into structural units including sentences, clauses, lists, tables, code blocks, and mathematical blocks.

### **HLR‑20.40.010‑002**  
SOB SHALL preserve the order of structural units exactly as provided in the input.

### **HLR‑20.40.010‑003**  
SOB SHALL identify list structures (ordered, unordered, nested) and represent them explicitly in the TP structure.

---

## **3.2 Modality Extraction Requirements**

### **HLR‑20.40.010‑004**  
SOB SHALL classify each structural unit by modality, including declarative, interrogative, imperative, conditional, and hypothetical.

### **HLR‑20.40.010‑005**  
SOB SHALL mark modality classifications in the TP structure without altering the original text.

---

## **3.3 Operator‑Hint Extraction Requirements**

### **HLR‑20.40.010‑006**  
SOB SHALL extract operator hints when present, including summarize, compare, classify, derive, plan, explain, rewrite, and transform.

### **HLR‑20.40.010‑007**  
SOB SHALL represent operator hints as residue fragments suitable for routing.

---

## **3.4 Domain‑Hint Extraction Requirements**

### **HLR‑20.40.010‑008**  
SOB SHALL extract domain hints when present, including math‑like, code‑like, narrative‑like, legal‑like, technical‑like, and conversational‑like.

### **HLR‑20.40.010‑009**  
SOB SHALL encode domain hints as residue fragments without interpreting domain semantics.

---

## **3.5 Tone‑Hint Extraction Requirements**

### **HLR‑20.40.010‑010**  
SOB SHALL extract tone hints when present, including formal, casual, technical, supportive, urgent, and neutral.

### **HLR‑20.40.010‑011**  
SOB SHALL encode tone hints as residue fragments without modifying the TP.

---

## **3.6 Constraint‑Hint Extraction Requirements**

### **HLR‑20.40.010‑012**  
SOB SHALL extract constraint hints when present, including precision, conciseness, politeness, safety, and formatting constraints.

### **HLR‑20.40.010‑013**  
SOB SHALL represent constraint hints as residue fragments for later processing by CnOB.

---

## **3.7 Structural Integrity Requirements**

### **HLR‑20.40.010‑014**  
SOB SHALL ensure that no structural unit is lost, merged incorrectly, or reordered.

### **HLR‑20.40.010‑015**  
SOB SHALL ensure that all extracted structural metadata is deterministic and reproducible for identical inputs.

---

## **3.8 Residue Formation Requirements**

### **HLR‑20.40.010‑016**  
SOB SHALL produce residue fragments representing unresolved structure, operator hints, domain hints, tone hints, and constraint hints.

### **HLR‑20.40.010‑017**  
SOB SHALL ensure that residue fragments are addressable by the routing block (RB) using the system’s geometric addressing scheme.

### **HLR‑20.40.010‑018**  
SOB SHALL ensure that residue entropy after SOB is strictly less than the entropy of the raw TP.

---

## **3.9 Routing Requirements**

### **HLR‑20.40.010‑019**  
SOB SHALL produce residue suitable for routing into SROB as the next classification layer.

### **HLR‑20.40.010‑020**  
SOB SHALL NOT route directly into semantic OBs or constraint OBs.

---

## **3.10 Prohibited Behavior Requirements**

### **HLR‑20.40.010‑021**  
SOB SHALL NOT perform semantic interpretation of any kind.

### **HLR‑20.40.010‑022**  
SOB SHALL NOT infer meaning, intent, or semantic roles.

### **HLR‑20.40.010‑023**  
SOB SHALL NOT apply constraints or enforce constraint logic.

### **HLR‑20.40.010‑024**  
SOB SHALL NOT modify the original text of the TP.

---

## **3.11 Discourse‑Context Propagation Requirements (Structural Only)**

### **HLR‑20.40.010‑030**  
SOB MAY consume CE‑derived and CEx‑derived discourse‑context metadata in a read‑only manner, including recent‑entity continuity, temporal markers, contrastive structural markers, and causal structural markers.

### **HLR‑20.40.010‑031**  
SOB SHALL encode discourse‑context metadata only as structural flags in residue and SHALL NOT perform semantic interpretation or intent inference.

### **HLR‑20.40.010‑032**  
Discourse‑context flags SHALL be deterministic, bounded, and reproducible for identical inputs.

### **HLR‑20.40.010‑033**  
SOB SHALL NOT modify TP text, semantic_core, FFTM fields, or any meaning‑layer representation when propagating discourse‑context metadata.

### **HLR‑20.40.010‑034**  
Discourse‑context flags SHALL be representable as addressable residue fragments suitable for routing by RB and refinement by SROB, STPX, and ISc.

### **HLR‑20.40.010‑035 — TP‑Stream Metadata Consumption**  
SOB SHALL consume applicable deterministic TP‑stream metadata when such metadata improves structural segmentation or hint extraction.

### **HLR‑20.40.010‑036 — Read‑Only Discipline**  
SOB SHALL treat all TP‑stream metadata as read‑only and SHALL NOT modify any TP fields.

### **HLR‑20.40.010‑037 — Metadata Applicability Rule**  
SOB SHALL consume TP‑stream metadata only when such metadata is relevant to structural classification and SHALL ignore metadata that is not applicable.

### **HLR‑20.40.010‑038 — Forbidden Metadata Consumption**  
SOB SHALL NOT consume routing_metadata, ΔH%, truth/done fields, lineage fields, or any Pipeline‑B envelopes.

### **HLR‑20.40.010‑039 — Structural‑Only Interpretation**  
SOB SHALL treat all consumed TP‑stream metadata as structural or semantic‑adjacent features and SHALL NOT perform semantic interpretation.

### **HLR‑20.40.010‑040 — Residue Integration**  
SOB SHALL encode any consumed TP‑stream metadata as structural residue fragments when such metadata affects segmentation or hint extraction.

---

## **3.12 New Requirements for Structural‑Importance and Bounded Semantic‑Adjacent Interpretation**

### **HLR‑20.40.010‑041**  
SOB SHALL detect candidate structural entities and anchors (including noun‑phrase‑like spans, subject‑like spans, object‑like spans, and header‑like spans) and emit them as structural‑importance residue fragments.

### **HLR‑20.40.010‑042**  
SOB SHALL compute structural‑importance values using deterministic rules based solely on structural and semantic‑adjacent cues, without performing semantic‑role assignment or referent resolution.

### **HLR‑20.40.010‑043**  
SOB SHALL treat structural‑importance detection as bounded semantic‑adjacent interpretation and SHALL NOT perform meaning construction, intent inference, or semantic‑role interpretation.

### **HLR‑20.40.010‑044**  
SOB SHALL ensure that structural‑importance residue fragments are addressable by RB and refinable by SROB, CnOB, and SmOB.

### **HLR‑20.40.010‑045**  
SOB SHALL ensure that structural‑importance residue fragments reduce entropy and improve downstream meaning extraction without modifying TP text or meaning‑layer fields.

---

# **4. Primitive Read/Write Tables (Explicit Fields, Envelope‑Grouped)**

Below are the deterministic read/write tables for every primitive.  
This version includes **CCR‑aligned importance emission** and **CCR output consumption**.

---


## **4.1 InB — Intake Buffer**

| Category | Reads | Writes |
|---------|-------|--------|
| Semantic | raw_input | messy_input_record |
| Metadata | — | signature_history[] (append) |
| Purpose | Capture raw input. |

---

## **4.2 IIInB — Intake Interpretation Buffer**

| Category | Reads | Writes |
|---------|-------|--------|
| Semantic | messy_input_record | proposition_set[] |
| Metadata | signature_history[] | entropy_history[] (append) |
| Purpose | Convert messy input into propositions. |

---

## **4.3 IE — Intake Envelope**

| Category | Reads | Writes |
|---------|-------|--------|
| Semantic | proposition_set[] | truth_evidence[] |
| Metadata | difficulty_rating, mismatch_tags[], anomaly_flags[] | delta_h_percent |
| Purpose | Form initial semantic envelope + entropy. |

---

## **4.4 CEx — Context Extractor**

### **Additional CCR‑Aligned Behavior**
CEx now also emits **IE structural hints** used by CEx‑CCR:

- `topic_hint`  
- `intent_hint`  
- `continuity_hint`  
- `reference_hint`  
- `register_hint`  
- `politeness_hint`  
- `direction_hint`  
- `coherence_hint`  
- `importance_hint`  
- `structural_phrases[]`

These hints are **bounded structural categories** and are not modified downstream.

---

## **4.5 CE — Context Engine**

| Category | Reads | Writes |
|---------|-------|--------|
| Context | topic, stance, intent, register, politeness, tone | continuity, direction, coherence, importance, clarifying_fields[] |
| Metadata | delta_h_percent | entropy_history[] (append) |
| Purpose | Build full context envelope. |

---

## **4.6 TPU — TP Update Engine**

| Category | Reads | Writes |
|---------|-------|--------|
| All envelopes | full TP snapshot | structural envelope (commit), metadata envelope (commit) |
| Purpose | Commit structural + metadata deterministically. |

### **TPU Commit Semantics**

| Envelope | TPU Commit? | Later Mutation Allowed? |
|----------|-------------|--------------------------|
| Structural | Yes | No |
| Metadata (entropy, signature, anomaly) | Yes | No |
| Metadata (context‑driven) | No | Yes |
| Semantic | No | Yes |
| Context | No | Yes |
| Identity | No | Yes |
| Provenance | No | Yes |

---

## **4.7 SOB — Structural Observation Buffer**

### **CCR‑Aligned Addition**
SOB may emit **semantic‑importance residues** when structural cues identify primary entities or objects.

Writes (additional):
- `TP.semantic.importance.entities[]` (append)

---

## **4.8 SROB — Structural Refinement Observation Buffer**

### **CCR‑Aligned Addition**
SROB may refine importance roles based on structural refinement.

Writes (additional):
- `TP.semantic.importance.entities[]` (update roles)

---

## **4.9 CnOB — Conceptual Observation Buffer**

### **CCR‑Aligned Addition**
CnOB may emit conceptual importance residues (issues, facts).

Writes (additional):
- `TP.semantic.importance.facts[]` (append)

---

## **4.10 SmOB — Semantic Observation Buffer**

### **CCR‑Aligned Addition**
SmOB may merge semantic geometry into importance residues.

Writes (additional):
- `TP.semantic.importance.entities[]` (update)  
- `TP.semantic.importance.facts[]` (update)

---

## **4.11 ISc — Initial Scoring**

| Category | Reads | Writes |
|---------|-------|--------|
| Semantic | smob_semantic_geometry | — |
| Metadata | delta_h_percent, entropy_history[], difficulty_rating, mismatch_tags[], anomaly_flags[] | delta_h_percent (update) |
| Purpose | Compute entropy and decide refinement vs termination. |

---

## **4.12 SSG — Semantic Stability Gauge**

| Category | Reads | Writes |
|---------|-------|--------|
| Semantic | smob_semantic_geometry | semantic_tags[] |
| Metadata | entropy_history[] | entropy_history[] (append) |
| Purpose | Assess semantic stability. |

---

## **4.13 STPX — Semantic Topology Extractor**

| Category | Reads | Writes |
|---------|-------|--------|
| Semantic | semantic_tags[] | semantic_tags[] (update) |
| Purpose | Extract semantic topology cues. |

---

## **4.14 RBU — Register/Belief/Usage Commit**

### **CCR‑Aligned Addition**
RBU may refine importance roles based on identity‑conditioned meaning.

Writes (additional):
- `TP.semantic.importance.entities[]` (update roles)

---

## **4.15 TR — Thought Router**

| Category | Reads | Writes |
|---------|-------|--------|
| Semantic | semantic_tags[] | — |
| Context | context envelope | routing_path |
| Metadata | delta_h_percent | — |
| Purpose | Produce routing vector. |

---

## **4.16 CTP — Contextual TP Update**

| Category | Reads | Writes |
|---------|-------|--------|
| Context | full context envelope | context envelope (update) |
| Metadata | entropy_history[] | entropy_history[] (append) |
| Purpose | Apply context‑driven TP updates. |

---

## **4.17 RTU — Routing TP Update**

| Category | Reads | Writes |
|---------|-------|--------|
| Metadata | delta_h_percent | delta_h_percent (update) |
| Provenance | routing_path | routing_path (append) |
| Purpose | Update TP based on routing vector. |

---

## **4.18 RB — Router Buffer**

| Category | Reads | Writes |
|---------|-------|--------|
| Semantic | semantic_tags[] | — |
| Context | context envelope | — |
| Metadata | delta_h_percent | — |
| Identity | cob_state_snapshot | — |
| Purpose | Decide termination vs refinement. |
*(unchanged)*

---

## **4.19 IdOB — Identity Observation Buffer**

### **CCR‑Aligned Addition**
IdOB may emit identity‑conditioned importance residues.

Writes (additional):
- `TP.semantic.importance.entities[]`  
- `TP.semantic.importance.facts[]`

---

## **4.20 OuBA — Output Buffer Assembly**

| Category | Reads | Writes |
|---------|-------|--------|
| All envelopes | full TP snapshot | TPSnS (all freeze fields) |
| Purpose | Produce final immutable meaning snapshot. |

---

# ⭐ **4.21 CEx‑CCR — Contextual Conversation Resolver (NEW)**

### **Reads**
- IE structural hints  
- `TP.semantic.importance`  
- static CIL substrate (`cil_input.yaml`)  
- CIL lineage + metrics  
- CIL semantic residues  

### **Writes**
- `TP.cex.ccr`  
- `selected_conversation` (consumed by COB)

### **Purpose**
- Computes six alignment dimensions  
- Copies CIL metrics  
- Applies deterministic decision rules  
- Selects conversation deterministically  
- Emits CCR envelope for CEx‑Pck and COB

---

# ⭐ **4.22 COB — Conversation‑Origin Buffer (Updated)**

### **Reads**
- `TP.cex.ccr.selected_conversation`  
- `TP.semantic.importance.entities[]`  
- `TP.semantic.importance.facts[]`

### **Writes (to CIL substrate)**
- `semantic_residue.important_entities[]`  
- `semantic_residue.important_facts[]`

### **Purpose**
- Projects importance residues into the selected conversation  
- Performs linear, deterministic copy‑forward  
- Does not infer meaning or transform values

---

# **5. Outputs (Normative)**

### **HLR‑20.40.010‑028**  
SOB SHALL output:

- structured TP  
- residue set  
- structural metadata  
- operator‑hint metadata  
- domain‑hint metadata  
- tone‑hint metadata  
- constraint‑hint metadata  
- structural‑importance metadata

### **HLR‑20.40.010‑029**  
SOB SHALL guarantee that its output is valid input for SROB.

---

# **End of Document — 20.40.010 (Updated)**

---
