# ⭐ **20.15 — Deterministic Simulation‑Ready Architecture Scaffold (Version 4.1)**  
### *Authoritative Architecture Specification for Path‑A*  
### *Aligned with CEx‑IE, CEx‑CCR, CEx‑Pck, CE, TP Metadata (20.105), IdOB 20.40.050 v3.1*  
### *Replay‑Deterministic, Simulation‑Deterministic, and Pipeline‑Deterministic*

**Status:** Informative (no SHALLs). v4.1 adds the official Path A primitive flow and the IdOB crossing packet on the TP.

---

# **1. Purpose**

This document defines the **complete deterministic architecture scaffold** required to run a full Path‑A simulation using only:

1. **This document**, and  
2. `path_a_full_context_test_run_cp.md`

Version 4.0 incorporated:

- **CEx‑IE structural hints**  
- **CEx‑CCR alignment + decision + selected_conversation**  
- **CEx‑Pck metadata packaging (20.107.030)**  
- **CE Envelope (20.108)**  
- **semantic‑importance residues**  
- **CCR output envelope (`TP.cex.ccr`)**  
- **CIL substrate integration**  
- **COB projection rules**  
- **updated TP metadata envelopes (20.105.*)**  
- **updated provenance discipline**  
- **updated deterministic ordering**  
- **updated freeze/commit semantics**  
- **updated refinement loop**  
- **updated early‑exit and termination rules**

Version 4.1 adds:

- official Path A primitive flow (IdOB after a committed RB cycle; looped refinement; OuBA exit)  
- `tp.idob` crossing packet, utterance carrier, CIE, meaning Δh ≠ entropy ΔH%  
- isolation fixture note (utterance/card without live RB) vs full-cycle schedule  

IMR is removed.  
All metadata formerly carried by IMR is now stored directly in TP.

Law for IdOB writes: `20.40.050`. Law for TP field slots: `20.105` / `20.105.010`.

---

# **2. Unified TP Datapacket (Version 4.1)**

All Path‑A data exists in a single TP datapacket, grouped into deterministic envelopes.

This version adds (4.0):

- **context_metadata** (from CEx‑Pck)  
- **msl_metadata** (from CEx‑Pck)  
- **cil_metadata** (from CEx‑Pck)  
- **semantic_residue_metadata** (from CEx‑Pck)  
- **next_context metadata**  
- **CCR output envelope (`TP.cex.ccr`)**

and (4.1):

- **utterance** — carrier string (not meaning)  
- **`TP.idob`** — IdOB structure-to-meaning crossing packet  
- **`TP.semantic.meaning_delta_h`** — meaning-layer Δh (not entropy ΔH%)  
- root flags `idob_complete`, `path_b_eligible`, `ready_for_ouba`

All TP fields MUST be emitted and consumed using canonical nested paths.
Flattening is a primitive-local operation and MUST NOT modify the TP envelope.

---

## **2.1 Semantic Envelope**
```
proposition_set[]
truth_evidence[]
semantic_tags[]
messy_input_record
meaning_delta_h          // IdOB meaning instrument; not ΔH%
meaning_semantics[]      // authorized export alias
idob_semantics[]         // authorized export alias
```

---

## **2.2 Context Envelope (Raw Context Fields)**
Produced by CEx‑Pck and consumed by CE:

```
topic
stance
intent
register
politeness
tone
continuity
direction
coherence
importance
clarifying_fields[]
```

---

## ⭐ **2.3 Context Metadata Envelope (NEW — CEx‑Pck → CE → Downstream)**

```
TP.metadata.context {
    context_fields { ... }        // full context envelope
    relevance_flags
    copy_forward_flags
    reset_flags
    context_provenance
}
```

This is the **canonical CE envelope** (20.108).

---

## ⭐ **2.4 Meaning Signal Layer (MSL) Metadata (NEW — CEx‑Pck)**

```
TP.metadata.msl {
    qualifiers[]
    clarifications[]
    stance
    shading
    intent
    direction
    coherence
    subculture
    msl_provenance
}
```

Consumed by IdOB, TR, RB, CIL, CST as **read-only cues**.  
CIE (Conversational Identity Envelope, `20.40.050` §0.6) is **not** this MSL block. CIE is the hop stance/hold applied to the six-axis stand-in after birth.

---

## ⭐ **2.5 CIL Metadata (NEW — CEx‑Pck)**

```
TP.metadata.cil {
    selected_conversation
    cil_reference
    projection_provenance
}
```

Consumed by COB, CIL, CST.

---

## ⭐ **2.6 Semantic‑Residue Metadata (NEW — CEx‑Pck)**

```
TP.metadata.semantic_residue {
    entities[]
    facts[]
    alignment_scores
    provenance
}
```

Consumed by COB, CIL, CST, IdOB (read-only / identity-adjacent).

---

## ⭐ **2.7 Semantic‑Importance Envelope (CCR‑Aligned)**

```
TP.semantic.importance {
    entities[]: { value, role, score, provenance }
    facts[]:    { value, role, score, provenance }
}
```

Produced by SOB, SROB, CnOB, SmOB, IdOB, OuBA, SSRGn.

Consumed by CEx‑CCR and CEx‑Pck.

---

## ⭐ **2.8 Next‑Context Metadata (NEW — CE → CEx‑Pck)**

```
TP.metadata.next_context {
    next_context
    direction
    coherence
    stance
    subculture
    next_context_provenance
}
```

Used by CEx‑Pck for continuity and MSL construction.

---

## ⭐ **2.9 CEx‑CCR Output Envelope (NEW)**

```
TP.cex.ccr {
    alignment { identity, clarifying, context, continuity, reference, semantic_residue }
    scores { ambiguity, collapse, drift, stability }
    decision
    selected_conversation
    provenance
}
```

Consumed by CEx‑Pck and COB.

---

## **2.10 Metadata Envelope**
```
delta_h_percent          // entropy / scoring trajectory — not meaning_delta_h
entropy_history[]
signature_history[]
policy_markers[]
difficulty_rating
mismatch_tags[]
anomaly_flags[]
```

---

## **2.11 Structural Envelope**
```
sob_structural_map
srob_structural_map
cnob_semantic_geometry
smob_semantic_geometry
```

Six structure IDs used by the IdOB crossing (field, role, object, gradient, universe, subfield) are **read** from this family or from an isolation card/pack assignment. IdOB does not mutate this envelope.

---

## **2.12 Identity Envelope**
```
cob_state_snapshot
TP.metadata.identity.*     // lifecycle / optional export (basin, subculture, …)
TP.idob                    // official S2M crossing packet (20.40.050, 20.105.010)
```

`cob_state_snapshot` is not the crossing packet. Lifecycle identity geometry is not required for an S2M hop to be legal.

---

## **2.12A IdOB crossing packet (v4.1)**

```
TP.idob {
    utterance
    card_id, assignment_status
    six structure IDs, structural_key
    candidate_group_ids, final_rank_order, selected_group_id
    cie_id, meaning_semantics, meaning_semantics_prime
    meaning_delta_h, meaning_cie_delta, first_meaning_cycle
    residue_code, identity_residual, hold_geometry
    resolution_status
    ready_for_ouba, path_b_eligible, idob_complete
    routing_filter_mutated
    expand_target          // hint only — not an RB next six-tuple
}
```

Plus root copies of the three flags and `TP.semantic.meaning_delta_h`.

---

## **2.13 Provenance Envelope**
```
lineage_log[]
sob_id
srob_id
cnob_id
smob_id
idob_id
routing_path
ruleset_ids[]
```

---

## **2.14 OuBA Freeze Fields**
```
tpsns_id
commit_timestamp
commit_hash
routing_epoch_id
```

---

# ⭐ **3. Path‑A Pipeline — official primitive flow (v4.1)**

This is the **official Path A lineup**. Every lineup diagram in this scaffold SHALL be read as this flow. Earlier compact walks in this file remain as historical CEx-era sketches; they do not override this section.

```
Path A Flow:
InB (Beginning of Path A) → IIInB → IE → CEx → CE → TPU
  → SOB → SROB → CnOB → SmOB
  → WrdNm → ISc → SSG → STPX → RBU → DCB
  → TR → CTP → RB
  → WrdNm → ISc → RTU → TR → CTP → RB
  → IdOB → MCB → RBU → DCB
  → TR → CTP → RB
  → WrdNm → ISc → RTU → TR → CTP → RB
  → IdOB → MCB → RBU → . . .
OR
DCB → TR → CTP → RB → WrdNm → ISc → RTU → TR → CTP → RB → OuBA (End of Path A)
```

Read of the official flow:

- IdOB runs **after a committed RB** (second RB on the first cycle, then again after later RB passes). That matches `20.40.050` scheduling law.  
- The loop `IdOB → MCB → RBU → … → IdOB` is serial meaning refinement (`20.40.050-004`).  
- OuBA is the **exit**, not a peer of IdOB on the same tick.  
- Isolation tests may feed IdOB an utterance/card fixture without executing the live RB to the left of it (`20.40.050-053`). That does not rewrite this flow.

### **CEx‑Pck Integration**
CEx‑Pck is the packaging step inside **CEx** (CEx‑IE → CEx‑CCR → CEx‑Pck) before CE.

### **CE Integration**
CE is the canonical context envelope consumed downstream (ISc and later readers).

### Historical compact walk (v4.0, kept)

```
InB → IIInB → IE → CEx‑IE → CEx‑CCR → CEx‑Pck → CE
→ TPU → SOB → SROB → CnOB → SmOB
→ ISc → SSG → STPX → RBU → TR → CTP → ISc → RTU → RB
→ (refinement loop if needed) → OuBA → TPSnS
```

Missing from that sketch: WrdNm placements, DCB, IdOB, MCB, the double RB before first IdOB, and the OuBA-only exit. Use the official flow above.

---

# ⭐ **4. Primitive Read/Write Tables (Updated for v4.1)**

### ✔ All primitives updated  
### ✔ All metadata boundaries updated  
### ✔ All deterministic ordering updated  
### ✔ All CCR/CEx‑Pck/CE integration updated  
### ✔ IdOB crossing row added  

---

## **4.4 CEx — Context Extractor (Updated)**

CEx now consists of:

1. **CEx‑IE** — structural hints  
2. **CEx‑CCR** — alignment + decision + selected_conversation  
3. **CEx‑Pck** — metadata packaging  

---

## ⭐ **4.5 CEx‑Pck — Packaging Layer (20.107.030)**

**Reads:**

- IE structural hints  
- CCR output  
- semantic‑importance  
- next_context  

**Writes:**

- context_fields  
- relevance_flags  
- copy_forward_flags  
- reset_flags  
- context_provenance  
- MSL metadata  
- CIL metadata  
- semantic‑residue metadata  

**Purpose:**

- deterministic metadata packaging  
- bounded‑semantic context construction  
- continuity + relevance + reset logic  
- CIL substrate selection  
- semantic‑residue alignment  

---

## ⭐ **4.6 CE — Context Engine (20.108)**

**Reads:**

- context_fields  
- MSL metadata  
- continuity flags  
- next_context  
- provenance  

**Writes:**

- canonical CE envelope  
- extraction_audit  
- ce_version_tag  
- timestamps  

**Purpose:**

- produce the canonical CE object  
- handoff boundary to semantics  
- deterministic context normalization  

---

## ⭐ **4.21 CEx‑CCR — Contextual Conversation Resolver**

Fully aligned with 20.107.030 and 20.108.

---

## ⭐ **4.22 COB — Conversation‑Origin Buffer**

Fully aligned with CEx‑Pck and CCR.

---

## ⭐ **4.23 IdOB — Identity Object Basin (20.40.050 v3.1)**

**Reads (full cycle):** committed routing view (`process.routing_filter` read-only), structural six-IDs / residue from OB-set, MSL / clarifying / importance as identity-adjacent cues, optional prior `TP.idob` / prior_M, `cie_id`.

**Reads (isolation):** utterance and/or `card_id`, `packs_loaded`, `cie_id`.

**Writes:** `TP.idob`, `TP.semantic.meaning_delta_h`, root `idob_complete` / `path_b_eligible` / `ready_for_ouba`. Optional lifecycle `TP.metadata.identity.*` when that export is enabled. `TPU.idob_update` is the commit request.

**Must not write:** `process.routing_filter`, DCB `geometric_state`, SSG / structural maps, next six-tuple as if it were RB.

**Purpose:** speaker-projection stand-in (six axes) + CIE pressure + meaning Δh + flags. Research visibility hop, not a claim that the packet is a person’s meaning-object.

---

# ⭐ **5. Deterministic Ordering (Updated)**

CEx‑Pck ordering is now canonical:

1. IE  
2. CCR  
3. semantic‑importance  
4. next_context  
5. context_fields  
6. MSL  
7. continuity flags  
8. CIL metadata  
9. semantic‑residue metadata  
10. provenance  
11. TP write  

CE ordering is canonical:

1. read CEx‑Pck metadata  
2. normalize context  
3. construct CE envelope  
4. write CE into TP  
5. produce extraction_audit  
6. version CE  

IdOB hop ordering (inside one IdOB box on the official flow):

1. utterance and/or card / packs → six IDs + key  
2. map door → rank ⊆ door  
3. prototype → \(M\)  
4. CIE → \(M'\)  
5. Δh vs prior_M or zeros  
6. flags + leftover residue_code  
7. write `TP.idob` / meaning Δh; restore routing_filter if touched  

---

# ⭐ **6. Updated Deterministic Rules**

All deterministic rules updated to reflect:

- CEx‑Pck metadata  
- CE envelope  
- CCR output  
- semantic‑importance  
- provenance discipline  
- freeze/commit boundaries  
- IdOB packet determinism (same utterance/card + packs + CIE + prior_M + YAML revision → same packet)  

---

# ⭐ **7. Updated Simulation Procedure**

Fully rewritten to reflect:

- CEx‑IE → CEx‑CCR → CEx‑Pck → CE  
- official Path A flow in §3 (including IdOB after RB, loop, OuBA exit)  
- deterministic metadata packaging  
- deterministic CE construction  
- deterministic structural pass  
- deterministic entropy evaluation  
- deterministic refinement loop  
- deterministic termination  
- deterministic freeze  

---

# ⭐ **8. Updated Early‑Exit and Termination Rules**

Aligned with:

- CE envelope  
- CEx‑Pck metadata  
- CCR output  
- semantic‑importance  
- provenance discipline  
- official OuBA exit of §3  

---

# ⭐ **9. Updated Downstream Consumption Map**

Explicit mapping of:

- CE → ISc  
- context_fields → TR/RB  
- semantic_residue → COB/CIL/CST  
- MSL → IdOB/TR/RB (read-only cues)  
- `TP.idob` / flags / meaning_delta_h → TR, RB, MCB, OuBA (read-only views)  
- provenance → TPU/OuBA  

---

# ⭐ **10. Updated Freeze/Commit Rules**

TPU commit semantics updated to reflect:

- CE envelope  
- CEx‑Pck metadata  
- provenance discipline  
- freeze/thaw behavior  
- `TPU.idob_update` committing `TP.idob` when the hop ran  

---

# ⭐ **End of Document — 20.15 (Version 4.1)**
