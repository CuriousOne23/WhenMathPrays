# 20.109 — IE Primitive (Rewritten)
**Document ID:** 20.109  
**Version:** 2.1 (TP Alignment Correction)  
**Date:** 2026‑07‑20  
**Status:** Active — Path‑A Primitive  
**Upstream:** 20.101_iiinb_prim.md  
**Downstream:** 20.107_cex_extract.md, 20.108_ce_envelope.md  

---

# 1. Purpose *(Informative)*

The Intake Envelope (IE) is the **final pre‑semantic representation** of the user’s input in Path‑A.

IE is the primitive that **first constructs the TP envelope** for Path‑A:

- IE consumes IIInB output.
- IE produces a **TP instance** with:
  - `TP.intake`
  - `TP.structure`
  - `TP.metadata`
  - `TP.error`

IE ensures:

### 1. All deterministic repairs are applied before semantic processing  
IIInB proposes repairs; IE applies them to produce **TP.intake**, a normalized, repaired surface form.  
This guarantees that CEx, CE, and downstream semantic modules operate on **clean, normalized input** free of surface defects or shorthand artifacts.

### 2. The input is structurally stable and schema‑validated  
IE enforces the canonical TP structural schema, including:

- normalized token sequence in **TP.intake.tokens**  
- deterministic whitespace and punctuation normalization  
- repair annotations in **TP.metadata.repair_annotations**  
- structural tags in **TP.structure.tags**  
- deterministic replay metadata in **TP.metadata.replay**

### 3. The input is fully deterministic and replay‑equivalent  
IE encodes all information needed for deterministic replay:

- repair operations  
- structural tags  
- token boundaries  
- normalization metadata  
- ruleset identifiers  

Replay systems can reconstruct the exact input state without external context, using **TP.metadata.replay**.

### 4. The input is safe for CEx and CE  
IE is the **last pre‑semantic boundary** before CEx.  
It ensures that CEx receives:

- no semantic content  
- no inferred meaning  
- no ambiguity resolution  
- no candidate expansions  
- no truth evaluation  
- no nondeterministic fields  

### 5. Path‑A begins semantic processing from a clean foundation  
IE bridges:

- **InB → IIInB** (canonicalization + repair proposals)  
- **CEx → CE → ISc → TPU → TP.semantic** (context extraction + semantic processing)

IE is a **data‑integrity primitive**, not a semantic module.

---

# 2. Position in Pipeline *(Informative)*

IE is produced:

- after InB → IIInB  
- before CEx  
- before any semantic interpretation  
- before `TP.semantic`, `TP.process`, or higher‑level `TP.metadata` are constructed

IE is the **first primitive that emits a TP envelope** for Path‑A.

---

# 3. IE Output Schema (Canonical TP Mapping) *(Informative)*

IE SHALL emit a TP instance with the following minimum fields:

```json
TP {
  intake: {
    tokens: [Token],
    normalized_text: string
  },
  structure: {
    tags: [StructuralTag],
    spans: [Span],
    markup: [MarkupIndicator]
  },
  metadata: {
    repair_annotations: [RepairAnnotation],
    replay: ReplayMetadata,
    ruleset_id: string
  },
  error: TPError | null
}
```

Where:

- `TP.intake` corresponds to the **normalized, repaired surface form**.  
- `TP.structure` corresponds to **structural tags and spans**.  
- `TP.metadata` corresponds to **repair annotations, replay metadata, and ruleset identifiers**.  
- `TP.error` corresponds to the **deterministic error envelope** for IE.

> **Note:** Field names and envelopes SHALL conform to the TP requirements in:
> - 20.105_tp_requirements.md  
> - 20.105.010_tp_meta_fields.md  
> - 20.105.020_tp_meta_provenance.md  
> - 20.105.030_tp_meta_usage.md  

---

# 4. Normative Requirements (HLRs)

## 4.1 Determinism Requirements

**HLR‑20.109‑001**  
IE SHALL be deterministically reproducible from the same IIInB output, emitting an identical TP instance.

**HLR‑20.109‑002**  
IE SHALL NOT include nondeterministic fields (timestamps, random IDs, environment‑dependent values) in `TP.metadata`.

---

## 4.2 Boundedness Requirements

**HLR‑20.109‑003**  
IE SHALL enforce bounded size limits on `TP.intake` as defined by system intake constraints.

**HLR‑20.109‑004**  
IE SHALL truncate or summarize oversized `TP.intake` fields using a deterministic, documented procedure, and SHALL record such truncation in `TP.metadata.replay`.

---

## 4.3 Replayability Requirements

**HLR‑20.109‑005**  
IE SHALL encode all information required for downstream components to replay the user’s input without accessing external state, stored in `TP.metadata.replay`.

**HLR‑20.109‑006**  
IE SHALL preserve ordering of user‑provided tokens, structures, and metadata in `TP.intake.tokens` and `TP.structure.tags`.

---

## 4.4 Structural Normalization Requirements

**HLR‑20.109‑007**  
IE SHALL represent the user’s input using the normalized structural schema defined by the TP specification.

**HLR‑20.109‑008**  
IE SHALL include structural tags (token spans, block boundaries, markup indicators) in `TP.structure.tags` when present in IIInB output.

---

## 4.5 Repair Integration Requirements

**HLR‑20.109‑009**  
IE SHALL incorporate all repair proposals from IIInB into `TP.intake.normalized_text` without semantic modification.

**HLR‑20.109‑010**  
IE SHALL mark repaired regions using `TP.metadata.repair_annotations`, including:
- repair type  
- target region  
- proposal text  
- any relevant provenance metadata  

**HLR‑20.109‑016**  
IE SHALL read `IIInB_Output.anomaly_flags` and SHALL surface each unresolved anomaly as an anomaly marker in `TP.metadata.repair_annotations` (or a dedicated anomaly‑provenance subfield), so downstream primitives can deterministically detect and propagate anomaly provenance.

> **Clarification:**  
> IE SHALL NOT introduce a separate `TP.repairs[]` list; anomaly and repair provenance SHALL be represented via `TP.metadata.repair_annotations` and related TP metadata fields as defined in 20.105.010/020.

---

## 4.6 Token Normalization Requirements

**HLR‑20.109‑011**  
IE SHALL contain a normalized token sequence in `TP.intake.tokens`, including whitespace normalization, canonicalization, and repair integration.

**HLR‑20.109‑012**  
IE SHALL preserve token boundaries exactly as defined by the IIInB tokenizer.

---

## 4.7 Structural Integrity Requirements

**HLR‑20.109‑013**  
IE SHALL validate its emitted TP instance against the TP schema before being passed to CEx.

**HLR‑20.109‑014**  
IE SHALL reject malformed or incomplete structures and return a deterministic error envelope in `TP.error`, without emitting a partially valid TP instance.

---

## 4.8 Prohibited Content Requirements

**HLR‑20.109‑015**  
IE SHALL NOT contain semantic interpretations, inferred meanings, candidate expansions, or truth‑evaluation artifacts.  
`TP.semantic` SHALL remain empty until CE → ISc → TPU.

---

# 5. Mathematical Form *(Informative)*

A simplified representation of IE’s output is:

\[
TP = \{ TP.intake,\ TP.structure,\ TP.metadata,\ TP.error \}
\]

Where:

- **TP.intake** = normalized token sequence + applied repairs  
- **TP.structure** = structural tags and spans  
- **TP.metadata** = replay metadata, ruleset identifiers, repair annotations, anomaly provenance  
- **TP.error** = deterministic error envelope (or null)

This is illustrative; the normative schema is defined in the TP and IE specifications.

---

# 6. Example *(Informative)*

An example TP emitted by IE may include:

- normalized tokens in `TP.intake.tokens`  
- repair annotations in `TP.metadata.repair_annotations`  
- structural tags in `TP.structure.tags`  
- deterministic replay metadata in `TP.metadata.replay`  
- no semantic interpretation; `TP.semantic` remains empty.

---

# End of Document — 20.109 (TP‑Aligned IE Primitive)
```

Key corrections:

- IE is explicitly defined as the primitive that **emits TP**, not a separate “IE envelope” with its own naming.
- All field names are expressed in terms of `TP.intake`, `TP.structure`, `TP.metadata`, `TP.error`, matching 20.105.*.
- HLR‑20.109‑016 no longer refers to `TP.repairs[]`; anomaly provenance is correctly tied to `TP.metadata.repair_annotations` / TP meta fields.
- Requirements that previously said `IE.intake`, `IE.structure`, `IE.metadata` are now mapped to TP, so InB/IIInB stay consistent and IE becomes the TP‑constructor primitive.

Once you’re happy with this 20.109 correction, we can then bring `ie_py_struc_pgm.md` and `ie.py` into alignment with this TP‑aligned spec.
