# ⭐ **20.109 — IE Primitive (TP‑Aligned)**  
**Document ID:** 20.109  
**Version:** 3.1  
**Date:** 2026‑07‑31  
**Status:** Active — Path‑A Primitive  
**Upstream:** 20.101_iiinb_prim.md  
**Downstream:** 20.107_cex_extract.md, 20.108_ce_envelope.md  

---

# **1. Purpose** *(Informative — Modified)*

The Intake Envelope (IE) is the **machine‑efficiency layer** between IIInB and all downstream primitives.  
IIInB performs **tokenization and token‑level classification only**; IE performs **packing and compression** of that information into the first committed TP envelope.

IE’s purpose is **not** to reinterpret input, re‑detect anomalies, or re‑tokenize.  
IE’s purpose is to **prevent downstream primitives from redoing IIInB’s work** by producing a compact, deterministic, machine‑efficient representation of:

- committed token sequence  
- token‑level normative classification  
- committed normalized surface (informational only)  
- committed structural interpretation  
- committed anomaly and repair provenance  
- committed replay metadata  

IE applies IIInB’s repair proposals **exactly**, without semantic expansion, and marks tokens as normative/repaired/anomalous so downstream primitives can operate **without re‑deriving anomaly information**.

IE is the **semantic compressor** of Path‑A:  
it transforms IIInB’s raw semantic material into a **downstream‑efficient TP envelope**.

---

# **2. IE Role in Path‑A** *(Informative — Modified)*

IE is the **first efficiency‑oriented primitive** in Path‑A.  
Its role is to take IIInB’s raw output — tokens, anomaly flags, repair proposals, structural tags — and pack them into a deterministic, compact TP structure that downstream primitives can consume **directly**.

IE performs:

1. **Deterministic repair application**  
   Applies IIInB repair proposals exactly as specified, producing committed tokens.

2. **Token‑level normative classification**  
   Marks each token as normative, repaired, or anomalous so downstream primitives do **not** need to re‑scan for illegal characters, determinism, or strange symbols.

3. **Machine‑efficiency packing**  
   Produces `TP.intake.tokens` + `TP.intake.token_flags` as the **primary machine substrate** for CE, CEx, ISc, TPU, and all downstream primitives.

4. **Normalization (informational)**  
   Constructs `normalized_text` for debugging, replay, and structural primitives, but downstream semantic primitives are **not required** to consume it.

5. **Structural construction**  
   Packs structural tags and spans into TP.structure without reinterpreting IIInB’s decisions.

6. **Replay determinism**  
   Encodes provenance so the TP envelope can be reconstructed deterministically.

IE does **not** modify upstream fields, does **not** infer meaning, and does **not** perform semantic interpretation.  
Its value is **efficiency**: downstream primitives operate on IE’s packed representation without redoing IIInB’s work.

---

# **3. IE Output Schema (Canonical TP Mapping)** *(Informative — Modified)*

IE emits a committed TP instance that is **optimized for downstream consumption**.  
The TP envelope produced by IE is intentionally compact: it contains only the fields downstream primitives need to operate deterministically and efficiently.

```json
{
  "intake": {
    "tokens": ["string"],          // raw IIInB tokens (read‑only for IE)
    "ie_tokens": ["string"],       // committed IE tokens (post‑repair, post‑normalization)
    "token_flags": ["TokenFlag"],
    "normalized_text": "string"
  },
  "structure": {
    "tags": ["StructuralTag"],
    "spans": ["Span"],
    "markup": ["MarkupIndicator"]
  },
  "metadata": {
    "repair_annotations": ["RepairAnnotation"],
    "replay": "ReplayMetadata",
    "ruleset_id": "string"
  },
  "error": "TPError | null"
}
```

### Efficiency Principle  
Downstream primitives SHALL treat:

- `tokens`  
- `token_flags`  

as the **primary machine‑efficient substrate**.  
Other fields (normalized_text, repair_annotations, replay, structure) exist for provenance, debugging, and structural primitives, but **do not require downstream semantic primitives to re‑interpret IIInB output**.

IE’s output is the **first fully packed TP envelope** in Path‑A.

---

# **3.1 TP Fields Used and Produced by IE (Normative)**

### **HLR‑20.109‑017 — TP Fields Read by IE**  
IE SHALL read only the following IIInB‑produced fields:
- TP.intake.surface  
- TP.intake.tokens  
- TP.repair_proposals  
- TP.anomaly_flags  
- TP.intake.metadata.iiinb  

### **HLR‑20.109‑018 — TP Fields Written by IE**  
IE SHALL write only:
- TP.intake.normalized_text
- TP.intake.ie_tokens
- TP.intake.token_flags
- TP.structure
- TP.metadata.repair_annotations
- TP.metadata.replay
- TP.metadata.ruleset_id
- TP.error

### **HLR‑20.109‑019 — Prohibition on Upstream Mutation**  
IE SHALL NOT modify:
- TP.intake.surface
- TP.intake.tokens
- TP.repair_proposals
- TP.anomaly_flags
- TP.intake.metadata.iiinb

---

# **4. Normative Requirements (HLRs)**

## **4.1 Determinism Requirements**

**HLR‑20.109‑001**  
IE SHALL be deterministically reproducible from the same IIInB output.

**HLR‑20.109‑002**  
IE SHALL NOT include nondeterministic fields (timestamps, random IDs, environment‑dependent values).

---

## **4.2 Boundedness Requirements**

**HLR‑20.109‑003**  
IE SHALL enforce bounded size limits on TP.intake.

**HLR‑20.109‑004**  
IE SHALL record deterministic truncation in TP.metadata.replay.

---

## **4.3 Replayability Requirements**

**HLR‑20.109‑005**  
IE SHALL encode all provenance required for deterministic replay.

**HLR‑20.109‑006**  
IE SHALL preserve ordering of tokens, structures, and metadata.

---

## **4.4 Structural Normalization Requirements**

**HLR‑20.109‑007**  
IE SHALL represent input using the normalized structural schema.

**HLR‑20.109‑008**  
IE SHALL include structural tags when present or derivable.

---

## **4.5 Repair Integration Requirements**

**HLR‑20.109‑009**  
IE SHALL incorporate all IIInB repair proposals without semantic modification.

**HLR‑20.109‑010**  
IE SHALL mark repaired regions in TP.metadata.repair_annotations.

**HLR‑20.109‑016**  
IE SHALL surface all anomaly_flags as anomaly‑provenance entries.

**HLR‑20.109‑027**  
IE SHALL apply repair proposals deterministically and without reordering.

**HLR‑20.109‑028**  
IE SHALL apply each repair proposal exactly to its specified span.

**HLR‑20.109‑029**  
IE SHALL preserve rule identifiers and spans for all applied repairs.

---

## **4.6 Token Normalization Requirements**

**HLR‑20.109‑011**  
IE SHALL produce a normalized token sequence including repair integration.

**HLR‑20.109‑012**  
IE SHALL preserve token boundaries except where repairs modify them.

**HLR‑20.109‑030**  
IE SHALL preserve token boundaries unless explicitly modified by repair.

**HLR‑20.109‑031**  
IE SHALL emit tokens suitable for deterministic replay.

---

## **4.7 Structural Integrity Requirements**

**HLR‑20.109‑013**  
IE SHALL validate TP against the TP schema.

**HLR‑20.109‑014**  
IE SHALL reject malformed structures and emit TP.error.

**HLR‑20.109‑032**  
IE SHALL construct TP.structure deterministically.

**HLR‑20.109‑033**  
IE SHALL record structural provenance in TP.metadata.replay.

---

## **4.8 Prohibited Content Requirements**

**HLR‑20.109‑015**  
IE SHALL NOT include semantic interpretations or inferred meanings.

**HLR‑20.109‑034**  
IE SHALL NOT modify upstream fields.

**HLR‑20.109‑035**  
IE SHALL NOT introduce semantic content.

**HLR‑20.109‑036**  
IE SHALL emit TP.error when deterministic construction fails.

---

# **5. New Requirements Introduced in Version 3.1**

## **4.9 Token‑Level Normative Classification**

**HLR‑20.109‑037**  
IE SHALL emit TP.intake.token_flags with one entry per token.

**HLR‑20.109‑038**  
IE SHALL classify each token as normative, repaired, anomalous, unrecognized, or null according to rule‑driven behavior defined in ie_rules.yaml.

**HLR‑20.109‑039**  
IE SHALL NOT drop or replace tokens unless explicitly directed by rule.

---

## **4.10 Rule‑Driven Anomaly Handling**

**HLR‑20.109‑040**  
IE SHALL resolve anomaly_flags using rule‑driven behavior defined in ie_rules.yaml.

**HLR‑20.109‑041**  
IE SHALL NOT perform spelling correction, shorthand expansion, repetition collapse, punctuation normalization, or Unicode normalization unless IIInB proposes the correction.

**HLR‑20.109‑042**  
IE SHALL record all anomaly resolutions in TP.metadata.repair_annotations.

---

## **4.11 Rule‑Driven Normalization and Spacing**

**HLR‑20.109‑043**  
IE SHALL construct TP.intake.normalized_text using rule‑driven whitespace behavior defined in ie_rules.yaml.

**HLR‑20.109‑044**  
IE SHALL insert or omit spaces between tokens according to rule‑driven behavior and SHALL NOT assume downstream primitives require spaces.

---

## **4.12 Downstream Consumption Model**

**HLR‑20.109‑045**  
IE SHALL produce TP.intake.ie_tokens and TP.intake.token_flags as the primary machine‑efficient substrate for downstream primitives. Downstream primitives SHALL treat TP.intake.tokens as raw IIInB tokenization and SHALL NOT rely on it as the committed intake token sequence.

**HLR‑20.109‑046**  
IE SHALL ensure normalized_text is suitable for structural primitives, replay, provenance, and debugging, but SHALL NOT require downstream semantic primitives to consume normalized_text.

---

## **4.13 Machine‑Efficiency Enforcement**

**HLR‑20.109‑047**  
IE SHALL encode normative classification so downstream primitives can ignore or down‑weight unrecognizable tokens without re‑deriving anomaly information.

---

# **6. Mathematical Form** *(Informative)*

$$
TP = \\{ TP.intake,\ TP.structure,\ TP.metadata,\ TP.error \\}
$$

IE produces the committed TP envelope by applying deterministic repair proposals, constructing committed tokens, classifying token normativity, generating structural interpretation, and encoding replay metadata.

---

# **7. Example** *(Informative)*

An IE‑emitted TP instance includes:

- committed token sequence  
- token‑level normative flags  
- committed normalized surface  
- repair‑provenance annotations  
- anomaly‑provenance entries  
- structural tags  
- deterministic replay metadata  

TP.semantic remains empty.

---

# **Summary of Changes (Informative)**

- Added token_flags field.  
- Added rule‑driven anomaly handling.  
- Added rule‑driven normalization and spacing.  
- Added downstream consumption model.  
- Added machine‑efficiency enforcement.  
- Added HLR‑20.109‑037 through HLR‑20.109‑047.  
- Updated IE semantics to Model A.  
- Updated IE role as first mild semantic primitive.  
- Updated IE as man→machine boundary.  

---

# **End of Document — 20.109 (TP‑Aligned, Version 3.1)**

---
